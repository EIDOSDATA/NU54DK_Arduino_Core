# T12 공통 기능 묶음과 T13 시험 조합 확정

2026-09-07 시작. 기준 source `27e0f257860f46bfd43a82742cf087be7eaa9493`, board
`fe65f2f0880bd05b32e562d9bf1ee59142b4f4d3`. 이번 요청은 T12 현재 결선 묶음 실기와
T13 시험 조합·추가 결선의 확정까지다. T13 soak, T12 전체, RC 또는 공개 완료가 아니다.

## 실행 범위와 현재 상태

| 작업 | 비중 | 상태 |
| --- | --- | --- |
| 범위·새 장치 식별 | 5% | 완료: 12:38:53 UTC exact UID·COM·SWD·입력 상태 새 관측 |
| GPIO/GPIOTE | 20% | exact 4e48252 GPIO 342·task 720·edge 1440, 총 기능 2502 PASS·입력 복귀 확인 |
| PWM 남은 기능 | 20% | exact 3334b17 steady675와 0db0689 추가모드288 PASS; 첫 terminal STOP Host 오판 원본 보존 |
| QDEC 추가 조건 | 15% | e521825 진단40 PASS 뒤 GPIO400/QDEC399로 중단. SAMPLE 독립 합산·read 전후 trace로 누락 경로 추가 진단 |
| I2S 연속·단방향 | 15% | exactb5c86a4 별도432/432 PASS·결선105·cleanup432·양쪽 입력 복귀 확인 |
| T13 조합·추가 GPIO 결선표 | 15% | [확정 계획](../../tests/hil/nu54dk/T13_PLAN.md)·32 단독/8 동시·생산 route Host PASS; 실기 미실행 |
| 증거·문서·최종 검사·push/CI | 10% | 진행 중 |

진행률은 이번 요청 범위의 산출물 비중이며 릴리스 전체 진행률이 아니다. 부분 구현은
해당 기능의 실기 완료 비중을 충족하지 않는다. 첫 실패와 교정 이후 exact 재검사를 구분한다.

2026-09-07 15:36:05 UTC에 4e48252 GPIO campaign을 완료했다. 뒤의 3334b17 신호 campaign은
steady 675조건 통과 후 finite repeats/end-delay 첫 실패에서 종료했다. 첫 실패와 새 판정의
재검사를 구별하며 GPIO 결과를 뒤의 image source로 소급 표기하지 않는다.

## 현재 결선과 실행 세션

사용자가 현재 17신호+GND, P1.06↔P1.07 두 선만 교차, P2.07/08 미연결,
양쪽 DAP UART 분리·SWD 연결을 다시 확인했다.

> 예, 현재 그대로이며 실행 중 유지합니다

후속 사용자 지시:

> 작업하면서 들어. 그냥 쭉 진행해. 나 HW 손대는거 없고, 잘거야

이 유지 지시에 묶인 별도 `v04-fixed-common-harness-session`을 사용한다. 만료는
2026-09-08 07:00 KST(2026-09-07 22:00 UTC)다. 기존 확인서의 30분 제한을 바꾸거나
사람 확인 시각을 반복 갱신하지 않는다. 새 세션은 고정 catalog·exact UID·board와
501/502/508/520/530 범위만 허용하며, 최대 12시간 제한과 wall/monotonic 만료를 적용한다.
각 새 clean image에서 controlled flash 후 501 결선 검사를 먼저 통과해야 기능 출력을 시작한다.
Probe 연결·runtime identity·명령 nonce/sequence를 확인하고 단절/오류 시 다음 case를 중단한다.
실행기 재시작을 끊긴 soak의 연장으로 취급하지 않는다. 보드의 10초 lease와 501의 500ms
LOW 해제는 유지한다. 장치 열거만으로 전압·물리 스위치 전체를 측정했다고 주장하지 않는다.

현재 읽기 전용 관측은 A COM12/13·B COM14/15, 양쪽 d8d1e13 runtime, CPUID
0x411fd210, SLEEPING, 17 PIN_CNF=0, PWM20/21/22 off·DPPI20 CHEN=0이다.
Raw UID는 공개 증거에 넣지 않는다. SWD 10 MHz·sector erase·auto_unlock=false·controlled
reset/halt/identity/start를 유지하며 recover/mass erase/무한 재시도를 사용하지 않는다.

## GPIO/GPIOTE 준비 계약

기존 501 catalog와 명령의 의미를 보존하고 별도 공통 기능 catalog와 502 명령을 추가한다.
GPIO는 Arduino ID와 물리 net을 구분하며 API input/pull/output/open-drain과 peer pad·API
readback을 비교한다. 종료 때 입력 전환과 내부 handover로 소유권을 반환한다.

GPIOTE20 8채널·GPIOTE30 4채널을 승인 net에 순환 대응한다. 양쪽 역할에서 OUT/SET/CLR,
rising/falling/toggle, 100/1000 edge/s의 유한 1000에지를 각 10회 검사한다.
CPU task 신호원과 event poll의 실제 count·최대 poll 간격을 기록한다. 정밀 timing 측정은 아니다.
한 번에 net 하나만 출력하며 비대상 전체 net의 방향과 다른 채널 이벤트 누출을 검사한다.
잘못된 pin·채널·port는 입력 또는 사전 거부 경로로 검증한다.

이전 501의 102+3 PASS는 [99번](99_공통_결선_검사와_승인_전_자동_진행_계획.md)에 보존한다.
현재 기능 코드의 PASS로 복사하지 않는다.

## 첫 실기와 Host 판정 교정

Exact 645df82에서 clean pair 2/2와 clean M27 9/9를 확인한 뒤 controlled flash했다.
501의 102 net-round와 3 timeout, 양쪽 502 negative 2묶음은 통과했다. 첫 GPIO
A P1.14 반복 1의 open-drain LOW에서 Host가 `PIN_CNF=0x801`을 잘못 해석해 중단했다.
원본과 양쪽 cleanup은 [첫 실행 manifest](evidence/t12-common-gpio-645df82-first/manifest.json)에 보존한다.

고정 SDK `nrfx/bsp/stable/mdk/nrf54l15_types.h`의 DRIVE0[9:8]=S0(0),
DRIVE1[11:10]=D1(2)를 대조하면 0x801은 올바른 open-drain 출력이다.
Host의 이전 3bit DRIVE 해석을 `PIN_CNF & 0xF00 == 0x800`으로 교정하고 실제 첫 raw와
push-pull·다른 drive 조합의 거부를 Host 회귀에 추가했다. 코어 수정이나 현장 결선 수리로
기록하지 않는다. GPIO 완결 case는 0이며 GPIOTE task/edge도 미도달이다.
13:03:21 UTC 읽기 전용 postflight에서 두 role/source, 17 PIN_CNF=0·PWM/DPPI off를 확인했다.

교정 source 4e4825279a9bd665221c40490c4d9c8781cd27ae를 C:/cgc04에 clean pair 2/2로 빌드했다.
13:07 UTC 후속 controlled flash 뒤 501 105개를 새로 통과하고 위 GPIO/GPIOTE 실행을 진행 중이다.

## 다음 PWM·QDEC·I2S 기능 준비

- 508 steady PWM은 승인된 축별 축소 675조건을 실제 runner에 적용했다. 45 instance/slot/load
  identity별 15조건이다. TOP·길이 모든 값과 duty×극성을 유지하고 최장/최저속을 교차한다.
  전체 2700 Cartesian이나 완전 pairwise와 동등하다고 주장하지 않는다. 이전 full vector는 보존한다.
- 508 추가 PWM은 sequence0의 25→50%, sequence1의 75→25% 순서,
  repeats/end-delay/playback 두 묶음(0/0/52의 208주기, 9/3/4의 172주기),
  CPU/EGU20→DPPI20 START, 시작 전 peer idle LOW/HIGH의 192조건이다. triggered-step 96조건은
  25/50/75% 각각 100주기 유지와 공개 step 호출만으로 전진하는지 검사한다. 원본 TIMER edge,
  이벤트 순서·count와 16-byte guard를 기록하고 START 구독을 먼저 해제한 후 STOP한다.
  마지막 LOOPSDONE→STOP은 마지막 frame의 REFRESH/ENDDELAY를 무시하는 hardware 계약을 따른다.
- 520 QDEC은 두 instance·debounce on/off·2/10ms state·1/100/1000회전·10반복의 240조건이다.
  N회전 정방향 뒤 QDEC를 계속 실행한 채 N회전 역방향, read/clear, 대각 전이 1회전,
  stop/restart 뒤 1회전을 구별한다. ACC/ACCDBL의 작은 hardware 범위를 5ms read로 합산하며
  read 간격 15ms 초과·포화 경계는 실패다. 긴 회전 수를 invalid/restart 1회전에도 수행했다고 표시하지 않는다.
- 530 I2S는 양 master 역할·16/48kHz·8/16/24/32bit·stereo/left/right·32/256/1024 word·
  A→B/B→A/full-duplex의 432조건이다. 3 DMA 슬롯을 순환하여 초기 4개+측정 100개를 보존한다.
  장치가 모든 활성 sample을 독립 pattern과 비교하고 Host는 104개 raw CRC/첫끝 word·전역 순서·
  측정 100개의 completion 간격을 대조한다. TX-only/RX-only는 비활성 DMA에 실제 nullptr를 사용한다.
  STOP 전 tail은 100개 측정에 더하지 않는다. 시작 zero는 최대 8개 정렬 frame만 허용한다.

준비 중 I2S의 pin_size_t narrowing과 PWM peer role의 null pointer 경고를 교정했다.
통합 첫 DUT link는 RAM 5968-byte 초과로 실패했다. I2S slot을 4→3, PWM raw capacity를
1025→513으로 조정하여 요구된 word/측정 buffer/100주기를 유지했다. C:/cao04의 최종 준비
pair build는 13:47:28 UTC **2/2 PASS**다. 앞선 build 실패 원본은 삭제하지 않는다.
새 기능의 Host 12개와 T13 생산 route/결선 Host 2개가 통과했다. 이 source는 4e48252 이후
미커밋 준비이며 실제 flash용으로 쓰지 않는다. 최종 source 고정과 clean pair build가 뒤따른다.

[준비 manifest와 실패 포함 원본](evidence/t12-common-signals-preparation/manifest.json)에
91그룹 715시험(713 PASS·설치 CLI/dirty M27 조건부 SKIP 2개), 계약·package 20개·inventory와
정렬 로그를 보존했다. 이후 faulted handle을 정상 cleanup으로 반환하지 않도록 HIL STOP을
보강했고 최종 clean target에서 다시 컴파일한다. 제품 core source·board gitlink는 바꾸지 않았다.

후속 준비 commit은 c215eb84164d0a97f7898ae24e1530aa93445fcd다. 실제 flash 전에 I2S
STOP 응답의 guard와 lease 오류도 필수로 확인하도록 Host cleanup을 보강했고, 한쪽 guard 실패여도
양쪽 STOP을 시도하며 정상 PASS로 끝내지 않는 회귀를 포함해 관련 Host 3/3을 통과했다.
이 보강은 새로운 물리 실패를 고친 기록이 아니며 새 clean build로 검사한다.

c77274c의 clean pair 2/2와 clean M27 9/9도 통과했고 아직 flash하지 않았다. 후속 Host 점검에서
긴 QDEC/GPIOTE 파형의 heartbeat 직전과 PWM async/step 중에도 고정 세션 연속성 검사를 추가했다.
만료 뒤 lease 갱신 없이 양쪽 STOP으로 빠지는 회귀를 포함해 공통 Host 14/14 PASS다.
현재 실기인 4e48252는 case 사이에 연속성을 확인하는 이전 실행이며 기능 관측을 소급 변경하지 않는다.
기존 신호 대기 launcher만 종료하고 새 source/clean build로 대체한다. GPIO 실기는 계속 실행 중이다.

1fc3962도 clean pair 2/2를 통과했으나 flash 전에 finite PWM 판정 계약을 더 점검했다.
sequence당 한 frame인 이전 104주기 준비안은 repeats와 end-delay의 합만 파형에 나타나므로
두 설정을 독립 검증하지 못했다. 각 sequence를 서로 다른 두 frame으로 변경하여 repeats는
모든 frame, end-delay는 마지막 frame에만 적용하는지 비교한다. 총 주기가 같아도 지연을
첫 frame에 잘못 더한 파형을 거부하는 Host 회귀를 포함한다. 최대 418에지는 capacity 513 이내다.
이전 준비 image와 실제 GPIO 관측은 그대로 보존하며, 변경한 source를 clean build한 뒤에만 사용한다.

## GPIO 완료와 첫 신호 실행의 판정 교정

4e48252의 [GPIO 전체 원본](evidence/t12-common-gpio-4e48252/manifest.json)은 기능 2502,
결선 105 PASS다. 1440 에지 조건에서 총 1,440,000 edge를 발생시키고 960,000 event를 받았다.
최대 poll 간격은 127µs이며 정밀 주파수 계측이나 T13 연속 soak로 취급하지 않는다.
15:36:10 UTC postflight는 양쪽 17 PIN_CNF=0, PWM/QDEC/I2S off, DPPI20/30 CHEN=0,
GPIOTE20/30 CONFIG=0, source/role 일치를 확인했다. session 5261은 정상 종료했다.

3334b1777327cc8106644a4c49760736d0f1d864의 clean pair 2/2(C:/cav04)와 Host
91그룹 717시험(716 PASS·설치 CLI 조건부 SKIP 1), 정렬 387개·contract/package/inventory/docs가
통과했다. 같은 시각의 전체 Host PASS를 실기 PASS로 바꾸지 않는다. 새 controlled flash와
501 105 PASS 뒤 steady PWM **675/675 PASS**를 확인했다. 다음 모드는 첫 0/0/52만 PASS,
9/3/4에서 `actual=172; expected=184`로 중단했다. [첫 신호 실행 원본](evidence/t12-common-signals-3334b17-first/manifest.json)
전체 상태는 failed로 보존하며 QDEC/I2S는 아직 실행하지 않았다. 양쪽 STOP 응답은 `[0,1,1]`,
후속 read-only postflight도 모든 pin 입력·주변장치 off였다.

[Nordic nRF54L15 decoder 규격](https://docs.nordicsemi.com/r/bundle/ps_nrf54l15/page/pwm.html-concept_wxj_hnw_nr)과
고정 SDK `nrfx/hal/nrf_pwm.h`의 `nrf_pwm_sequence_t` 주석은 복합 sequence의 마지막
LOOPSDONE에서 REFRESH/ENDDELAY가 적용되지 않음을 명시한다. production wrapper는 nrfx의
LOOPSDONE→STOP shortcut을 사용한다. 첫 raw의 duty별 길이는 25%×10, 50%×13, 75%×10,
중간 25%×23(앞 sequence 마지막 13+다음 처음 10), 마지막 25%×1이다. 중간 반복/지연은
정상이며 최종 9+3주기만 Host가 잘못 더했다. 제품 driver를 수정할 근거는 없다.

Host 기대값을 184→172로 교정하고 최초 raw·마지막 12주기를 잘못 더한 mutant·중간 지연 배분
오류를 회귀에 넣는다. 이미 통과한 steady는 3334b17 증거로 유지하며 다음 clean source에서는
`--section additional-signals`로 288모드→QDEC 240→I2S 432를 새로 실행한다.
새 flash와 501·연속성·cleanup은 생략하지 않는다. 원본 실패를 새 PASS로 덮지 않는다.

## 추가 PWM 완료와 QDEC 초기 상태 첫 실패

0db0689b6fcf2264748406947bb9dd23575abce3의 clean pair 2/2(C:/caw04), 관련 Host15/15 뒤
새 controlled flash·501 105 PASS를 확인했다. [추가 실행 원본](evidence/t12-common-additional-0db0689-first/manifest.json)의
PWM 288/288은 모두 PASS다. 최초 terminal STOP 오판을 고친 새 실기이며 이전 675조건과 source를 구분한다.
QDEC은 18조건 PASS 후 `20/debounce0/2000us/100cycles/repeat9/initial`에서 -1(예상0)을
검출해 중단했다. I2S는 미도달이며 전체 campaign은 failed로 보존한다.

직전 repeat8의 정상 정·역400 step, double2, restart4는 모두 통과했다. 그 cleanup은 B를 먼저
입력으로 바꾸고 A를 뒤에 정지했다. 이 사이 A의 raw는 추가 누산1과 phase B=1을 기록했고,
다음 B의 00 재설정 후 A 시작값은 -1이었다. QDEC 입력은 고정 SDK에서 NOPULL이며 decoder는
직전 sample과 현재 sample을 비교한다. 따라서 먼저 부유시킨 HIL 종료 순서가 측정 외 전이를
허용한 것이 우선 수정 대상이다. 현 단계에서 사용자 결선 불량이나 제품 driver 결함으로 단정하지 않는다.

Host cleanup을 **A 수신 정지→B 발생기 정지·입력 반환**으로 변경한다. A STOP 실패여도 B STOP은
시도하며 실패를 숨기지 않는다. 초기값0·정역합0·double2·재시작4의 기준을 낮추거나 bias를 빼지 않는다.
최초 -1 raw가 여전히 거부되는 회귀와 종료 순서·A 실패 시 B 반환을 검사한다. 다음 clean source에서
`--section qdec-i2s`로 QDEC 전체240과 I2S432를 새로 실행한다. 통과한 PWM 모드는 0db0689 증거로 남긴다.
후속 postflight는 두 보드의 17 GPIO 입력·PWM/QDEC/I2S/DPPI/GPIOTE off를 확인했다.

## QDEC 1스텝 누락의 독립 진단 준비

b5c86a49d2a2161fe5fa137609ed78a18e2925b7의 clean pair 2/2(C:/cax04), 관련 Host16/16 뒤
새 controlled flash·501 105 PASS를 확인했다. [재검사 원본](evidence/t12-common-qdec-b5c86a4-first/manifest.json)은
26조건 PASS 뒤 `20/debounce0/2000us/1000cycles/repeat7/forward`에서3999(예상4000)를
기록해 중단했다. 초기 누산은0, 송신 발생수4000·최종 phase00, 수신 double0·최대 read 간격5031µs다.
송신 완료 뒤4ms와 추가 READCLRACC 뒤에도3999였다. 양쪽 STOP과 read-only postflight는 PASS다.
이 실패는 이전 -1 초기 상태와 구별하며 전체 QDEC240을 완료로 표시하지 않는다.

후속 HIL은 같은 port IN을 한 번에 읽어 Gray 전이를 소프트웨어로 독립 누산한다. 최대 poll 간격과
안정 phase1024µs 이후 최초 GPIO/hardware 불일치의 register20개를 고정 보존한다.
원래 hardware 판정·read 주기·파형·기대값은 바꾸지 않는다. 이 계측은 고장 위치를 좁히기 위한
진단이며 배선 불량·코어 결함·READCLRACC race 중 어느 하나가 원인이라고 아직 단정하지 않는다.
Host는16가지 전이·cycle wrap·clear의 phase 보존을 실제 C++ observer로 검사한다.
진단 image를 새 clean build하기 전에는 flash하지 않으며 I2S 실행 중 두 probe를 중복 점유하지 않는다.

f46bf60의 첫 observer clean pair2/2는 build-only로 통과했다. Flash 전 진단 record의 SAMPLE 대신
마지막 공개 read 반환값을 보존하도록 보강한다. 최초 불일치 당시 `ACCREAD` register와 드라이버
반환값이 같은지도 대조하기 위함이다. 진단 page0은 현재 상태, page1은 최초 불일치다.
각20 word는 cycle/GPIO signed수/double수/전이수/최대pollµs/phase/phase유지µs,
software합산/ACC/ACCREAD/ACCDBL/ACCDBLREAD/read횟수/직전read간격µs/마지막read반환값,
SAMPLEPER/SHORTS/ENABLE/INTENSET/DBFEN 순이다. 원래16word 상태와 판정 계약은 유지한다.

I2S432는 b5c86a4 image와 `--section i2s`로 별도 controlled flash·501 뒤 실행 중이다.
QDEC 원인 분석 때문에 독립 I2S를 미실행으로 남기지 않으며 실제 결과는 완료 후 따로 기록한다.
b5c86a4 전체 Host gate는16:13:31 UTC에 exit0으로 끝났다. 실행 중이던 Host/실기 세션을
현재 실행 중으로 잘못 넘기지 않는다.

## I2S 현재 묶음 완료와 QDEC 진단 실행

b5c86a4의 [I2S 원본과 감사](evidence/t12-common-i2s-b5c86a4/manifest.json)는432조건 전체 PASS다.
각 master 역할216조건, 세 방향별144조건이며 실제 inactive DMA는 nullptr다. 초기4개+측정100개의
양쪽 완료89,856개 중 수신 buffer59,904개에서 **52,394,496 samples**를 비교했다.
측정 DMA 완료86,400개와 Host 관측 시점의 추가 tail52,114개를 구분한다. 최대 queue204µs,
가장 큰100-buffer completion span의 공칭 주기 편차4.9243%는 선언한5% 이내다.
정밀 clock 교정이나180/900/3600초 soak의 증거로 확대하지 않는다.

고유 Cartesian432개·양 역할·raw 순번·cleanup432개를 별도 감사했고 최초 실패는 없었다.
SWD10MHz 새 controlled flash·501105 PASS 뒤 수행했으며 종료 후 양쪽 identity와17 PIN_CNF=0,
PWM/QDEC/I2S off·DPPI20/30 CHEN=0·GPIOTE20/30 CONFIG=0을 확인했다. session74696은 종료했다.

e5218255b3796d660a62da7c2dc518b6c1371b66의 [software 원본](evidence/t12-common-readback-software-e521825/manifest.json)은
Host91그룹720시험(719 PASS·설치 Arduino CLI 조건부 SKIP1), pair2/2(C:/caz04), 정렬388개,
contract45·package20·inventory·docs PASS다. 제품 core·board·SDK·공개 자산은 바꾸지 않았다.
최신 observer source를 고정한 clean 검사이며, 앞서 진행 중 source가 바뀐 f46bf60 준비 Host를
이 source의 exact 결과로 사용하지 않는다. e521825 DUT RAM255872B/262144B, peer251752B다.

위 software archive의 별도 원본 감사는 PWM288 고유 vector를 대조하고 finite192조건에서
종료 pad가 설정한 LOW96/HIGH96으로 복귀했음도 확인했다. 이는 실행 후 raw 감사이며
원래 실행 Host가 final idle을 직접 assert했다고 소급 기록하지 않는다.

이후 session25455가 e521825의 새 controlled flash·501을 거쳐 QDEC240과 독립 GPIO 진단을
실행한다. 기존3999 실패의 원인 수정 완료나 새 QDEC 전체 PASS는 아직 주장하지 않는다.

## QDEC GPIO 입력 정상·누산 누락 관측

[e521825 진단 원본](evidence/t12-common-qdec-e521825-diagnostic/manifest.json)은501105와
QDEC40조건 PASS 뒤 `20/debounce0/10000us/100cycles/repeat1/forward`에서399/400으로 중단했다.
기존b5c86a4의2ms/1000회전 실패 지점은 이 실행에서 통과했으나 해결 완료로 해석하지 않는다.
마지막 GPIO observer는400전이·signed400·double0·최대poll101µs, QDEC합산399·double0이었다.
완료한41개의 진단 page 중 최대 GPIO poll 간격은104µs다. 최종 phase00은 유지됐다.

최초 불일치는326번째 전이에서 GPIO326·QDEC합산325+ACC0, phase3·유지1033µs로 기록됐다.
그 시점 마지막 read 뒤988µs, read횟수674, 마지막 반환값0·ACCREAD0, SAMPLEPER1(256µs),
SHORTS0·ENABLE1·INTENSET4(ACCOF만)·DBFEN0이었다. 수신 GPIO가 놓친 파형을 QDEC 누락으로
오인한 경우와 구별할 근거다. 이 한 snapshot만으로 SAMPLE 판독·누산기 또는 task/read 동작
중 어느 지점이 원인인지 확정하지 않는다. 양쪽 STOP·read-only postflight는 PASS, session25455 종료다.

다음 HIL 진단은 비활성 IRQ의 SAMPLERDY flag와 SAMPLE signed값을 별도 누산하고 관측 간격을
기록한다. 계측 간격이 샘플 주기보다 길어지는 경우 SAMPLE 합산을 독립 확정 근거로 쓰지 않는다.
원래5ms 공개 read 앞뒤의 ACC/ACCREAD·반환값과 GPIO/SAMPLE 합산을16개 ring에 저장하며
최초 불일치에서 ring을 고정한다. SAMPLERDY flag 관측 외 hardware 설정·파형·read 주기·판정은 유지한다.
제품 core 또는 SDK를 수정하거나399를400으로 보정하지 않는다.

명령85의 page2는 현재 SAMPLE signed합/double합/event수/최대관측간격µs/마지막관측cycle/
마지막SAMPLE/trace누계/다음ring index의8 word와 최초 불일치의 같은8 word다.
`85(3,offset)`은 offset0/2/…/14에서2개 record를 반환한다. 각 record10 word는
cycle/read순번/반환값/read직전ACC/직후ACC/직후ACCREAD/SAMPLE합/GPIO합/phase유지µs/마지막SAMPLE이다.
실패 원본→진단→ring→A STOP→B STOP의 Host 회귀를 포함해17/17 PASS다.

## T13 조합·추가 결선 확정

[GPIO 전체 결선표·복구 판정](../../tests/hil/nu54dk/T13_PLAN.md)와
[실행 계획 JSON](../../tests/hil/nu54dk/v04_t13_topologies.json)을 추가했다. 동시 8개 중 C05만
3600초이며 나머지 7개는 900초, 단독 32개는 각 180초다. 순차 연속 측정은 4시간 21분이며
구현·복구 100회·결선·실패 조사는 별도다. 현재 C→S(P1.04/05 및 P2.02/04 교차),
S→U(UART00용 P2.00/02 및 P2.04/05 교차) 두 후속 단계가 필요하다.
단일 재결선으로 UART00·SPI00·PDM·모든 동시 조합을 완료할 수 있다고 안내하지 않는다.

실제 production `serial_fabric_routes.cpp`를 Host에서 컴파일하여 모든 계획 serial endpoint를
검사했다. 잘못된 P2 직결 SPI/UART는 거부하고 두 보드의 중복 GPIO/serial block을 검사했다.
이는 고정 board descriptor 조건의 정적 허용 증거이며 동시 DMA/clock/IRQ 실기 PASS가 아니다.
S/U는 현재 공통 catalog 허용 대상이 아니며 사람의 새 결선 확인 후 후속 runner를 준비해야 한다.

## 준비 검사와 이전 커밋 원격 CI

준비 원본은 [manifest](evidence/t12-common-preparation-27e0f25/manifest.json)에 보존한다.
전체 Host와 문서 gate가 성공했고 C/C++ 381개 정렬을 통과했다. 초기 pair 준비 build는 2/2이며
마지막 negative 명령 추가 전 build이므로 source 고정 후 clean pair를 다시 빌드해야 한다.
이 준비 evidence의 source는 27e0f25 기반 미커밋 변경이며 실기 image로 사용하지 않는다.

27e0f25의 [Software](https://github.com/EIDOSDATA/NU54DK_Arduino_Core/actions/runs/34118608639)와
[Reproducible Builds](https://github.com/EIDOSDATA/NU54DK_Arduino_Core/actions/runs/34118608640)는
2026-09-07 12:46 UTC 조회에서 completed/success다. 이번 변경의 후속 CI와 구분한다.

이전 PC의 문서 인계 commit f42bda55f7b45f7af0a347ecf5f1f516bb34eb6a도
[Software 7개](https://github.com/EIDOSDATA/NU54DK_Arduino_Core/actions/runs/34096227435)와
[Reproducible 8개](https://github.com/EIDOSDATA/NU54DK_Arduino_Core/actions/runs/34096227297),
총 15/15 completed/success임을 새 PC에서 다시 확인했다.

## SAMPLE와 누산 차이 보존 및 계측 영향 대비

[8dbed87 원본·16-read 분석](evidence/t12-common-qdec-8dbed87-sample/manifest.json)은
새 controlled flash·501105 PASS 뒤 QDEC54조건 PASS, 다음20/db0/10ms/1000회전/repeat5의
정·역 최종 누산1(예상0)로 종료했다. C:/cbq04 clean pair2/2·관련 Host17/17·pair18/18이다.
GPIO4925전이의 signed3075·SAMPLE 합3075·double0과 공개 read합3076+ACC0이 처음 달랐다.
그 시점 GPIO 최대poll105µs, SAMPLE 최대관측간격343µs다. 관측 시간 변동을 고려한 한 샘플
누락 최소간격512−105=407µs보다 짧다는 것이 이 구간 SAMPLE 근거의 품질 조건이다.
최근16회 반환값과 직후ACCREAD는 모두 일치했다. 마지막 read 때 GPIO3075/SAMPLE3076,
반환0·ACC0이었다가 이후 SAMPLE만3075가 됐다. 이 결과는 단순한 반환값 stale load를
입증하지 않으며 READCLRACC와 샘플 누산의 겹침 가능성을 후속으로 조사한다.
최종 page0의0은 실패를 검사한 clear 명령 뒤 상태이며 실패값1을 보정한 결과가 아니다.
양쪽 STOP과 read-only postflight는 PASS, session30574는 종료했다.

다음 `qdec-read-diagnostic`은 기능240 PASS를 대체하지 않는 계획된80회 대비다.
같은 image·QDEC20/db0/10ms/100회전400전이에서 계측0(추가 read 없음),1(직후ACCREAD),
2(직전ACC·직후ACC/ACCREAD),3(2+SAMPLE polling)를 각20회씩 회전 순서로 비교한다.
5ms 공개 read, GPIO observer, 기대400/double0, 파형과 cleanup은 같다.
mode0/1에서 읽지 않은 trace register는0x80000000 sentinel로 표시한다.
mode 확인은85(page4)의mode/SAMPLEpoll/preACC/postACCREAD 네 word다.
예상 숫자 차이는 failed 원본으로 보존하고 양쪽 STOP·lease·identity·GPIO 조건이 확인된 경우에만
다음 계획 조건을 실행한다. 제어·누산 간격·파형·cleanup 오류는 즉시 중단한다.
마지막에 숫자 실패가 하나라도 있으면 전체 진단도 failed다. 맞는 비교는 observation이며
functional_regression=false로 남긴다. 제품 core·SDK를 수정한 우회나 기능 PASS 주장이 아니다.


## 계측 대비80회 결과와 clear 방법90회 진단 계획

[2359360 계측 대비 원본](evidence/t12-common-qdec-2359360-observation/manifest.json)은
501105 PASS와80회 모두400전이 일치로 끝났다. 계측 mode별20회, 총32,000전이다.
양쪽 STOP·postflight PASS이며 session29393 종료다. 전체 Host source도2359360으로 고정했다.
이 짧은 대비에서 간헐 누락을 재현하지 못했으며, 기존8dbed87 누락의 해결·기능240 PASS가 아니다.

다음 `qdec-clear-diagnostic`은 동일 QDEC20/db0/256µs sample·10ms phase·400전이에
clear 방법0(공개 READCLRACC),1(진단 전용 RDCLRACC→RDCLRDBL),2(이동 중 clear 없이
완료 후 공개 read)를 각30회, 총90회 회전 순서로 비교한다. 방법0/1은128µs 주기를 목표로
읽어 샘플과 clear가 겹칠 기회를 늘린다. 일반 기능 시험의5ms read는 그대로다.
방법1은 공개 API 검증이 아니며 SDK 수정도 아니다. 방법2는400전이가 누산 범위 안에 있다는
조건에서만 쓰는 진단이며6초 미판독 또는 ACC 절댓값900·ACCDBL14에서 gate를 닫는다.
이 제한은 진단에만 적용되고 일반 drain15ms·포화 오류 기준은 유지된다.
85(page5)는 방법/공개read사용/주기read사용/최대read간격µs/설정read주기µs의5 word다.
추가 방법은83(instance,debounce,계측3,방법)에서만 선택한다.

GPIO400/double0·최대poll128µs 미만과 독립 SAMPLE400/double0·최대간격384µs 미만을
함께 요구한다. 모든 숫자 실패는 raw/trace를 남기고 양쪽 STOP이 확인된 경우만 계획을 계속한다.
제어·관측 품질·overflow/시간·cleanup 실패는 즉시 종료한다. 비교 일치는 observation,
숫자 실패가 하나라도 있으면 전체 진단 failed이며 T12 기능 PASS에 합산하지 않는다.

QDEC 누산 clear90회(5/30·5/30·0/30 불일치), Nordic 공개 자료 대조와 다음 timing 대비는 [101번](101_T12_QDEC_누산_누락_원인_분리.md)에서 이어간다. session44561은 실패 원본·양쪽 STOP/postflight를 보존하고 종료했다.
