# v0.4.0 T01~T09 시험 준비와 구현 대조

| 항목 | 내용 |
| --- | --- |
| 기준 source | `8503e3dd95ffc2fab9c5844c3a550fbf50507deb` |
| Board gitlink | `fe65f2f0880bd05b32e562d9bf1ee59142b4f4d3` |
| 작성일 | 2026-09-04 |
| 작업 상태 | 진행 중 — T01 목록/누락 검사, T02 대조 및 T03 실행기 준비 |
| 사용자 승인 범위 | T01~T09, 두 USB 보드의 무배선 시험, 문서·commit/push. 상호 결선은 T10 안내 뒤 |

## 1. 준비와 실기 상태의 분리

[시험 목록](<../01_아두이노 코어 설계/12_v0.4.0_기능_시험_목록.md>)은 75개 identity와 19개
시험 family를 연결한다. JSON의 숫자는 초기 engineering 기준이며 기존 실측값이나 최고 속도
보증이 아니다. API/clock/배선 성립 조건을 대조하고 executable vector로 고정하기 전까지 T01은
부분 준비다. `verify_v04_test_plan.py` 성공은 대상 누락·문서 drift 검사이며 물리 PASS가 아니다.

현재 source/build/semantic 표시는 inventory의 이전 근거를 인용한다. 최신 실제 온보드 근거는
[41번](41_M24_M26_온보드_protocol_교정과_실기_재검증.md)의 `51c1986`에 한정된다.
새 계획의 1MHz UART, flow/error, 100회 반복, 600초/7,200초 soak는 아직 수행하지 않았다.

## 2. 후보 구현·공개 경로·남은 일

| 기능 | 소스·API 근거 | 기존 근거 / 공개 경계 | T04~T07·T16에 필요한 보완 |
| --- | --- | --- | --- |
| UART 5개 | `SerialFabric.h`, `UarteFabric.cpp` | 후보 async TX/RX·2-buffer. 기존 Serial/Serial1은 별도 production 경로. 온보드 20/21/22/30의 32B만 실기 PASS | 전 instance peer, baud/parity/RTS·CTS/error, 순환 refill·cancel/restart, 양방향 독립 판정 |
| SPIM/SPIS 각 5개 | `SpimFabric.cpp`, `SpisFabric.cpp` | source/build 계약. 기존 SPI singleton은 SPIM00 | controller/peripheral 실제 CS·mode·bit order·비대칭 길이·buffer turnover. `sync`는 SPIM에만 적용 |
| TWIM/TWIS 각 4개 | `TwimFabric.cpp`, `TwisFabric.cpp` | PMIC read-only TWIM20/21/22 기본 PASS. 기존 Wire는 TWIM22 | peer 주소 0x42/0x43, repeated-start, target requests, clock stretch/NACK/복구. PMIC 공유 bus에 1MHz·오류 주입 금지 |
| SAADC | `AnalogFabric.h/.cpp` | 8개 AIN metadata와 scan/differential/internal API, 기존 analogRead는 제한된 공개 route. 내부 VDD 단발만 PASS | 외부 8채널 route 안전성, manual/DPPI sampling, scan 단위·calibration gap·반복 buffer 수명. 내부 timer interval은 SDK 최대 128us, scan은 interval=0 필요 |
| PWM 20/21/22 | `AnalogFabric.cpp` | 각 4출력 sequence API, base clock 1MHz. 기존 analogWrite/tone/Servo는 별도 소유권 | 12출력의 개별 route/capture, load별 값 개수, TOP/주기, DMA 최대 count와 manifest 15bit 경계 대조 |
| TIMER 7개 | `EventFabric.h/.cpp` | 00/20~24는 CC 6개, 10은 8개로 구현. 모든 CC 실기 완료 아님 | 각 CC compare/capture, 자동 clear/stop, 다른 domain 연결, 충돌·해제 회귀 |
| EGU/GPIOTE/DPPI/PPIB | `EventFabric.h/.cpp` | EGU10/20, GPIOTE20/30, DPPIC 4개, PPIB 8개. channel/group 수 일부는 초기화 시 HAL에서 재조회 | PPIB 양 끝 연결, 모든 가용 channel·group, 예약 channel 거부. 초기 table의 group_count=0을 최종 하드웨어 능력으로 해석하지 않음 |
| GRTC | `libraries/NUCODE_NU54DK/src/NUCODE_NU54DK.h` | 기존 counter/alarm/wake API 존재. `EventFabric` factory에는 GRTC typed endpoint 없음 | 기존 API 회귀와 새 고급 endpoint 요구를 분리. 단순 handle 연결 완료로 보고하지 않음 |
| GPIO 0/1/2 | `variant.h`, `digital_pins.inc`, `nu54dk-arduino-pins.dtsi` | port identity와 모든 pad의 공개 사용 허가는 다름 | 가용 pin과 금지/공유 pin을 분리, input/output/open-drain·GPIOTE 연결·충돌 검사 |
| PDM20/21 | `StreamFabric.h/.cpp` | capture·queueBuffer·event API. handle 연결만 기존 build에 포함 | clock 추종 합성 peer, mono/stereo/edge·density, underrun/overflow와 재사용. SDK `NRFX_PDM_MAX_BUFFER_SIZE=32767` samples, completion event samples는 현재 0이므로 buffer 주소→등록 길이 대조 필요 |
| I2S20 | `StreamFabric.h/.cpp` | master/slave, 8/16/24/32bit, TX/RX/duplex, double buffer 후보 | 실제 clock 계산 성공과 packing·좌우·frame 순서 검증. 양쪽 동일 오류를 숨기는 echo만 사용하지 않음 |
| QDEC20/21 | `StreamFabric.h/.cpp` | accumulator/report·sample API, DMA 없음 | quadrature 생성, count 단위·방향·double-transition·report queue 검증 |
| M26 16개 | `system-capability-contract.json` | 전수 disposition 완료. TEMP/WDT30 기본 PASS, 기존 BLE·WDT31·power는 과거 근거 | 변경 영향 회귀. silicon-only·not-applicable은 원래 근거 유지, raw security/NFC/RADIO 시험으로 확대하지 않음 |

모든 후보의 설치 profile 통합은 T16에 남아 있다. `standard/prj.conf`에 production UART/Wire/SPI/ADC/PWM이
켜져 있다는 사실은 새 Fabric API가 일반 사용자 설치본에서 이미 활성화됐다는 뜻이 아니다.
75개별 source/exposure/build/semantic/HIL/concurrent 축은 생성 시험 목록 §4에서 누락 없이 추적한다.

## 3. 안전·기능 준비 이슈

| ID | 발견·추적 항목 | 후속 작업과 완료 조건 |
| --- | --- | --- |
| PREP-01 | `DISABLE_UART`는 DAP level shifter U6/U7의 두 UART를 함께 차단 | T03: UART 비의존 제어 채널과 정확한 UID·image 확인. UART가 없어도 board role/결과 식별 가능 |
| PREP-02 | AIN0~3은 DAP, AIN4/P1.11은 PMIC INT, AIN5/P1.12는 VBAT monitor, AIN6은 button, AIN7은 LED 공유 | T05/T08: ADC 입력이라는 이유로 peer push-pull HIGH를 강제로 연결하지 않음. 보호·개방드레인 또는 관측 경로의 기대값을 별도로 정하며 불성립은 HOLD |
| PREP-03 | 1MHz I2C와 stuck-low 오류 주입을 PMIC에 적용할 수 없음 | T04/T08: 독립 승인 route/pull-up 계획 필요. PMIC 기본 PASS로 peer·고속 기능을 대신하지 않음 |
| PREP-04 | DMA lease manager는 overlap/overflow를 검사하지만 일반 RAM 적합성·driver별 정렬은 별도 문제 | T07: 각 adapter→nrfx의 RAM/정렬/MAXCNT 검사 책임을 확정하고 안전한 거부부터 검사. 무효 DMA 주소를 실기에 전달하는 시험 금지 |
| PREP-05 | PDM clock 추종과 I2S packing oracle은 새 peer 구현이 필요 | T06: 합성 신호 생성 firmware·독립 oracle·단위 시험·build 완료 전 외부 시험 준비 완료로 처리하지 않음 |
| PREP-06 | GRTC 고급 endpoint와 기존 counter/alarm은 동일 API가 아님 | T05/T16: 요구 범위 대비 누락 추적, 기존 기능만으로 advanced parity 완료 선언 금지 |
| PREP-07 | 12 PWM 출력·다중 stream의 peer 관측 자원은 무한하지 않음 | T07/T08: 단독·동시 topology별 pin/channel/RAM 예산을 고정. 불가능한 전체 동시 연결을 안내하지 않음 |
| PREP-08 | 기존 runner가 단발 PASS 파일만 남기는 경로가 있음 | T03: 실패·timeout·중단 결과도 보존, stale 완료 재사용 거부, 두 UID 배타 점유와 role/nonce 검사 |

## 4. 보드와 회로도 대조

읽기 전용 조회에서 기존 DUT UID 끝 `fd7d106d`는 COM5/COM6, peer 끝 `c887aaf1`은 COM7/COM8로
동시에 열거됐다. COM 번호 자체를 역할의 영구 ID로 사용하지 않는다. 이 조회는 SWD 응답·flash·시험 PASS가 아니다.

회로도 9페이지 전체를 PNG로 렌더해 읽었다. Page 1/2의 모듈 pin과 공유선, 3의 전원/PMIC,
6의 UART/SWD enable, 7의 I2C pull-up, 8의 button/LED, 9의 header를 대조했다.
원본 PDF에 이미 일부 `Cannot open file` 그림 placeholder가 있고 Poppler가 Symbol/ArialUnicode
대체 font 경고를 냈다. 중요한 연결선·pin 표·스위치 회로는 판독되며 PDF 자체는 수정하지 않았다.

- 두 보드는 각자 USB 전원 사용. 공통 GND와 승인 signal만 연결하고 VBUS/VEXT/VBAT/3.3V/VDD_MOD끼리 연결하지 않는다.
- P2.6~P2.10의 금지 dedicated bank, PMIC 제어선, LFXO pad를 임의 구동하지 않는다.
- `DISABLE_SWD`는 UART 차단과 별개다. 스위치 silk의 ON/OFF 추정 대신 회로의 SWD 연결 유지/필요 UART 분리 조건을 안내한다.
- **이 문서는 아직 최종 T10 결선표가 아니다.** T04~T08에서 실제 시험 firmware의 pin·role·전압 조건을 고정한 뒤 연결표를 제공한다.

## 5. 진행 중 검사

- 시험 plan 누락 검사: 75 identities / 19 families, physical executed 0.
- 신규 plan Host 회귀: 10/10 PASS.
- 이후 build·실기·CI·commit 근거는 작업 묶음 완료 시 추가한다. 아직 T01~T09 전체 완료 아님.

### 첫 통합 검사 실패와 수정

`866852f`의 첫 target 검사(`C:/nb/06`)는 기존 18개 build-only 성공, 새 pair 2개는
SPIS compatible node 부재로 Kconfig 단계에서 실패했다(408.70초). 기존 M24 SPI overlay와
같이 disabled SPIS node를 명시했다. Driver를 켜거나 warning 전체를 무시하지 않는다.
Host 전체 검사에서는 v0.4.0 suite 수의 옛 기대값 18과 새 실제값 20이 달라 실패했다.
기대값을 20으로 갱신하고 재검사한다. 실패한 image의 flash는 수행하지 않았다.

추가로 controlled reset 직후 SRAM의 identity/request/response marker를 HALTED 상태에서
지워 이전 boot의 ready 값이 새 시작 확인으로 오인되지 않도록 했다.

### 두 보드 진단 — 단독 PASS와 통합 실패를 분리

`34475a0`은 Host 전체, 문서 142개, inventory와 target 20/20 build-only를 통과했다
(451.47초, `C:/nb/07`). Pair preflight에서 두 exact UID·role·HEX/ELF/source를 확인했다.
첫 통합 HIL은 DUT ping 뒤 PMIC 100kHz 첫 전송에서 `stop_timeout`으로 중단됐다.
SWD와 CPU는 응답했고 CFSR/HFSR는 0이었다. 동일 source의 기존 단독 TWIM20/21/22
400kHz 시험은 다시 3/3 PASS다. 따라서 USB 인식 실패나 보드 고장으로 확정하지 않는다.

별도 진단에서 두 보드 모두 TIMER00은 10,000us에 약 80,024 ticks, TIMER10은 약
20,005 ticks, TIMER20은 약 10,003 ticks를 반환했다. 분주 계산이 모든 instance에
16MHz base를 가정한 결함을 확인해 실제 HAL base clock으로 계산하도록 수정했다.
128/32/16MHz와 범위·2의 거듭제곱 분주 경계는 production helper를 Host C++로 검사한다.

내부 VDD 단발은 두 보드 모두 4092 raw와 정상 STOP을 반환했다. 뒤이어 VSS 시작은
실패했다. nrfx의 공통 enum 전체가 nRF54L15 입력은 아니며, pinned SDK의 이 칩 mapping은
VDD·AVDD 및 제공되는 DVDD다. 불가능한 입력은 configure 단계에서 거부하고 무배선
ADC 시험은 VDD/AVDD로 교정했다. 이는 VSS를 구현했다고 보고하는 변경이 아니다.

통합 I2C 실패 원인은 계속 분리 시험한다. 새 통합 image의 400/100kHz와 반환 코드를
명시하고, 물리 HIL 결과를 보존한다. 빠른 수정 확인은 `run_zephyr_build.py --suite`로
두 role만 선택할 수 있으나 evidence gate를 `m12-zephyr-build-subset`으로 구분하며
최종 전체 group 회귀를 대체하지 않는다.

보존 근거: [target 20개 build](evidence/34475a0/target-build.json),
[통합 I2C 실패](evidence/34475a0/pair-onboard-failed.json),
[두 보드 진단값](evidence/34475a0/pair-diagnostic.json),
[단독 TWIM 대조 PASS](evidence/34475a0/twim-control.json).
