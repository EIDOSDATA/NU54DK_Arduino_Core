# T14 PWM 지연 시작 취소와 무점퍼 검증

2026-09-07. 사용자가 보드 간 결선을 해제하고 자동 진행 가능한 범위의 실행을 지시했다.
PWM 미시작 STOP 결함 수정·회귀와 T12 잔여 준비, 소프트웨어 검증·문서·commit/push가 범위다.
외부 GPIO 결선·물리 재연결이 필요하면 해당 실기를 보류한다.

## 수정과 소프트웨어 재현

고정 SDK의 nrfx PWM은 START_VIA_TASK 준비만으로 POWERED_ON 상태가 된다. 실제 시작이
없으면 STOPPED를 받지 못해 기존 Fabric stop이 timeout했다. Host fake가 미시작 PWM에도
STOPPED를 주던 가정을 교정해 실제 production 경로를 실행했다. 새 8개 회귀 중 수정 전
6개 실패, 수정 후 8개와 기존 Analog/Stream 26개가 통과했다.

새 실행 전에 nrfx IRQ가 소비하지 않는 DMA READY·SEQSTARTED 흔적을 지운다. 지연 시작이고
시작 흔적이 없을 때만 PWM을 disable하고 다시 확인해 준비를 취소한다. disable과 DMA 시작이
겹치면 일반 STOP 확인 경로를 사용하며 timeout에서는 lease를 유지한다. 취소 과정에서
사용자 sequence를 강제로 시작하지 않는다. 미시작 취소는 logical stopped event를 전달하며
하드웨어 STOPPED 발생으로 기록하지 않는다.

외부 START DPPI 구독은 연결 소유자가 먼저 해제해야 한다. 구독이 남으면 stop은
ownership_conflict로 거부한다. START 주소는 해당 active 실행 동안만 유효하며 CPU/ISR의
raw START와 stop을 동시에 호출하지 않는 계약을 header에 명시했다. SDK·board·공개 API
signature는 유지한다.

[Nordic PWM register 설명](https://docs.nordicsemi.com/r/bundle/ps_nrf54l15/page/pwm.html-topic)의
DMA READY는 pointer/count 수신, SEQSTARTED는 최초 PWM 주기 시작을 나타낸다. 두 종류를
함께 사용해 DMA 준비와 파형 시작 사이도 일반 STOP/lease 보호 경로에 남긴다.

## 실행 상태

- 기준 HEAD: f17e603fe101a2296398f160cb81a29a54f15671. 현재 기록은 수정 중 checkpoint다.
- work/t14-pwm-deferred/before-fix.log: 새 8개 중 6 FAIL, 기존 26개 PASS.
- after-fix.log: production 회귀 34개 PASS. nojumper-oracle.log: 독립 판정 7개 PASS.
- host-precommit.log: 새 canonical target 추가 뒤 고정 suite 개수 검사 실패. 27→28을 반영했다.
- host-precommit2.log: 전체 Host gate PASS. 이후 추가한 독립 판정 7개는 위 별도 결과다.
- C:/u4n: 전용 target 1/1 build-only PASS. 이후 identity 보완의 exact rebuild가 필요하다.
- 두 지정 보드에서 SWD 10 MHz 읽기 preflight PASS, PWM20/21/22 ENABLE=0 확인. flash/reset 없음.
- **새 PWM 실기는 아직 미실행이다.** 검증한 clean source를 고정하고 exact image를 빌드한 뒤 진행한다.

## 실기 계획과 경계

[전용 프로그램](../../tests/zephyr/m25_nojumper_hil/README.md)은 P1.14 한 출력만 사용한다.
보드당 기본 960개와 취소/시작 방식별 100회 반복 12개를 실행한다. 두 보드의 원본 요청·응답,
nonce·source·image hash·guard·pin/DMA/block lease·주변장치 off를 독립 대조한다.
기존 PDM 결선 확인을 사용하지 않는다. 자동 mass erase/recover와 무한 재시도는 금지한다.

이 결과로 PWM peer capture·주기/듀티 오차나 T12 전체를 완료 처리하지 않는다.
남은 외부 T12 기능과 T13 이후는 [93번 목록](93_Host_재검증과_T12_이후_남은_작업.md)을 따른다.
