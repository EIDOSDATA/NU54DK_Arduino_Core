# 전체 문서 정리와 T13 S 잔여 재개

## 요청과 실행 경계

사용자가 전체 문서를 검토해 불필요한 내용을 정리하고 루트 README부터 가독성을 개선한 뒤,
안내한 S 순서1~3을 자동으로 가능한 만큼 진행하도록 지시했다.
이는 이전 실기 중지를 해제할 근거다. 문서 정리를 먼저 마치고 현재 장치·결선 조건을 대조한 후
I2S B 복구, 남은 통신 오류 주입, System OFF와 기존 요구별 구현·증거 대조를 진행한다.

연속 UART/SPI/TWI personality·역할 전환과 해당 전환을 반복하는 timing 진단은 제외 상태다.
QDEC 재진단·이미 완료한 정상36항목 재실행·자원 충돌의 생략한900회는 예약하지 않는다.
U 결선 변경과 T22 공개 승인은 이번 자동 실행에 포함하지 않는다.

## 문서 검토

시작 소스는 `ae526602145a2c89e4d609ea1dd68f63ec55cbff`이며 작업 트리는 깨끗했다.
자체 Markdown222개를 본문·목차·중복 문단·오래된 상태 표현으로 전수 조사했다.
활성57·생성10·검증 기록109·버전별 보존46개로 분류했고 여러 문서에 반복된 문단59개를 찾았다.
생성 문서와 SDK/서드파티는 직접 편집하지 않는다. 원본 결과와 버전별 문서는 보존한다.

주요 조치는 다음과 같다.

- 루트 README: 설치·지원 범위를 먼저 제시하고 오래된 실기 진행 보고를 제거한다.
- TODO: 현재 상태·S 잔여 순서·제외 조건·실행 경계를 앞에 모으고 시각별 과거 일지를 제거한다.
- 인계: 현재 진행 보고의 복사본 대신 새 PC 저장소·도구·보드 복원 절차로 정리한다.
- 문서/검증 목차: 목적별 입구와 전체 기록 링크를 제공한다. 과거 기록은 접어서 찾을 수 있게 한다.
- 설계·HIL·리팩토링 안내: 중복 현황을 TODO 링크로 바꾸고 과거 결과는 검증 원본을 참조한다.
- T13 계획·복구 안내: 제외한 연속 전환을 현재 실행 명령이나 완료 조건으로 안내하지 않는다.

내용이 겹치는 진행 문단을 삭제·통합하며 고유한 기술 계약과 실패 원본을 불필요한 문서로
분류하지 않는다. 기존 검증109개와 버전별46개는 이번 가독성 정리에서 원본 byte를 유지한다.
변경 전 문서와 전체 감사 목록·전후 해시는 별도 보존하고 검사 결과와 함께 등록한다.

## S 자동 진행 순서

| 순서 | 작업 | 준비 시점 상태 |
| --- | --- | --- |
| 1 | I2S B 공급 중단·정상 복구 |5c4c3e0의 두 역할 target·관련 Host10·원격 전체 Host829 준비, 새 실기0회 |
| 2 | UART CTS·parity/break·RX 공급 지연, SPIS 짧은 DMA/미준비, TWI stuck-low·TWIS write 공급 지연 | 기존54조건 미완료. 수정된 판정과 exact 소스·이미지 확인 후 독립 예행부터 수행 |
| 3 | System OFF bridge·timer/GPIO wake, 기존 CS 조기 종료·TWIS read·GPIO/event/PWM 충돌 요구 대조 | 실제 OFF 성공0회. 도구·기존 증거와 남은 구현을 분리해 처리 |

현재 S 유지 확인은2026-09-08 22:13:18 KST부터 다음 날10:13:18 KST까지다.
장치·USB·결선 변경과 확인서의 범위를 대조한다. SWD10MHz·exact UID·controlled sector flash·
`auto_unlock=false`, campaign별 전체 결선 검사와 양쪽 STOP·clock·GPIO 반환을 유지한다.
유효한 사용자 확인을 임의로 연장하거나 이전 COM 번호를 새 식별로 사용하지 않는다.

## 결과와 다음 행동

문서 정리를 완료했다. 기존222개를 전수 조사하고26개를 정리했으며 이번 기록1개를 추가했다.
문서·CI 계약·inventory/생성 문서·기능 시험표·C/C++ 정렬 검사를 통과했다.
[전수 감사·변경 전 문서·검사 원본](evidence/v040-doc-review-ae526602/manifest.json)에
문서별 보존/정리 판단과 전후 해시를 등록했다. 생성10·검증109·버전별46개는 원본을 유지했다.
문서 정리 커밋은 `9f8ca8b888249ef54e0f26ad8bb7a6db20fec089`이며 원격 main에 반영했다.
23:56 KST의 새 장치 열거에서 양쪽 exact UID hash와 A COM12/13·B COM14/15를 대조했다.
기존 S 확인의 시작·만료를 그대로 유지한 채 실제 실행을 재개했다.
T13 전체·RC·정식 공개는 미완료다.

## I2S B 재개 원본과 판정 보완

실행 source는 `5c4c3e0d21efa42209a814beab6367377a3b020a`, build는 `C:/t5f04`다.
23:57:17~23:57:52 KST [예행1회](evidence/t13-s123-i2s-b-preflight-5c4c3e0/manifest.json)는 통과했다.
23:58:25~23:59:14 KST [정식 실행](evidence/t13-s123-i2s-b-100-5c4c3e0/manifest.json)은
6회 통과 뒤7회차의 의도한 B 공급 중단 구간에서 실패했다. 두 campaign의 결선105항목과
양쪽 STOP·clock0·최종 핀 반환을 확인했다.7회차는 정상 재시작에 도달하지 않았다.

7회차 B는4버퍼 완료·5버퍼 제출 뒤 요청1회를 생략했고2,200µs 후 실제 underrun을 보고했다.
A의 첫 손상 index1278은 제출 끝1280의 마지막 stereo pair 시작이다. 기대2140809709와
실제2140798976은 상위 bit가 같고 하위 bit만0이며 나머지255word는 전부0이다.
STOP 뒤 읽은 양쪽 TX RAM의3slot 모두 원래 seed 패턴과 같았다. DMA RAM 손상은 관측하지 않았다.
이 결과를 이전 정상 재시작 중1word 오류나 첫 버퍼 index22 오류의 해결 증거로 사용하지 않는다.

Nordic의 [송수신 시점 설명](https://docs.nordicsemi.com/r/bundle/ps_nrf54l15/page/i2s.html-concept_wvj_ssy_vr)은
TXPTRUPD가 마지막 데이터의 직렬 송신 완료 전에 발생하며 FRAMESTART는 완전한 좌우 sample pair에
정의됨을 설명한다. 고정 SDK의 nrfx_i2s.c는 다음 버퍼 미제출 시 현재 버퍼를 재사용하고 NULL release를
통지한다. 따라서 제출량을 실제 핀의 마지막 bit 완료량으로 간주한 판정에는 차이가 있다.
현재 원본은 해당 마지막 frame의 STOP 절단으로 해석할 수 있으나 외부 파형을 계측한 결과는 아니다.

보완은 현재32-bit stereo fixture의 마지막2word 중 첫 word 절단도 분리한다. 정확한2word 시작 지연,
4완료/5제출의 생략·오류 원본, 전체 반환 RX와 MSB 보존·이후 전부0을 동시에 요구한다.
더 이른 손상·임의 bit 변경·비zero 꼬리·다른 생략 상태는 계속 거부한다. 원시 오류6과 정상 stream
비통과 표시는 보존하며 양쪽 종료와 새 seed의 정상 재시작을 통과해야 복구1회로 센다.
Host11시험과 저장 원본7회 재판정은 통과했다. 이 재판정은 새로운 실기 PASS가 아니며 새100회가 필요하다.
[RAM·재판정·종료 감사·Host 원본](evidence/t13-s123-i2s-stereo-analysis-5c4c3e0/manifest.json)을 보존했다.

## 9일00:17 KST 실행 체크포인트

- UART20 A CTS: source5c4c3e0 예행 후100/100회 완료. 실제100ms 정지·재개와 새 seed의 정상
  송수신,203개 양쪽 cleanup·최종 핀 반환을 확인했다. 기존54조건 중 이1조건의 완료다.
- I2S B: sourceafb5431의 exact target2/2·Host11·원격 전체 Host110그룹830시험 뒤
  [새 예행](evidence/t13-s123-i2s-stereo-preflight-afb5431/manifest.json)은 통과했다.
  [100회 실행](evidence/t13-s123-i2s-stereo-100-afb5431/manifest.json)은55회 성공 후56번째
  정상 재시작에서 A RX가 index100405부터201word 불일치로 중지했다. 최초 기대935317752,
  실제931135488 이후 zero 꼬리다. B는 당시 engine 활성·정상, 자체 RX 불일치0을 보고했다.
  양쪽 TX3slot RAM은 seed와 같았고 양쪽 종료·clock0·핀 반환을 확인했다. lease 갱신 거부는
  A가 데이터 오류로 먼저 멈춘 후의 표면 오류이며, 사용자 확인서 만료나 실제10초 lease 만료는 아니다.
  공급 중단 판정과 다른 정상 구간 오류이므로 허용 범위를 넓혀 PASS로 바꾸지 않는다.
- System OFF: source5c4c3e0 전용 power2/2·관련 Host7 후
  [bridge](evidence/t13-s123-power-bridge-5c4c3e0/manifest.json)는 실패했다. 실제 OFF 진입0회다.
  B는 예정 reset 뒤 uptime3ms, A는uptime28700ms에 error event5/mask12를 기록했다.
  양쪽 RX는LOW·TX는HIGH, TX/RX PSEL38/39, CONFIG4096, BAUDRATE30801920, XO.STAT0이었다.
  mask12는 framing/break이며 아직 payload DMA를 보내기 전이다. 양쪽 STOP stamp와17핀 입력을 확인했다.

System OFF 보완은 전용 UART RX에 내부 pull-up을 적용하고 적용 전후 PIN_CNF·실제 수준을
별도 SRAM에 보존한다. [Nordic의 RX LOW/break 설명](https://devzone.nordicsemi.com/f/nordic-q-a/77085/what-causes-a-uarte-internal-error)을
참고한 유휴선 보완이며, 실제 재실기 전에는 원인 확정이나 해결로 표시하지 않는다. 오류를 지우고
자동 재전송하는 변경은 하지 않는다. UART API 전역 정책과 일반 S 펌웨어에는 적용하지 않는다.

현재 UART21 A CTS부터 독립 조건 실행을 계속한다. I2S 부분55회·이전6회·예행을 합쳐100회로
세지 않는다. System OFF는 bridge 성공 전 timer/GPIO wake를 실행하지 않는다.

## 순서3의 기존 요구와 증거 대조

아래는 T13_PLAN에 이미 적힌 요구의 대조 결과다. 새로운 시험 범위를 추가하거나 완료한 T12를
다시 여는 표가 아니다. 요구 대조를 마친 것과 해당 T13 실기를 통과한 것을 구분한다.

| 기존 요구 | 확인한 구현·증거 | 아직 증명하지 않은 부분 |
| --- | --- | --- |
| SPI CS 조기 종료 | `v04_t13_fault.py` mode3과 `serial.cpp`가 controller의 도중 취소·부분 TX/RX AMOUNT·다음 seed frame을 검사한다. 기존 고정 serial21/21에 포함 | 기존 원본에는 peer CS 조기 에지와 불완전 slave frame의 독립 판정이 없다. short/unready10조건으로 대체하지 않음 |
| TWIS read 공급 지연 | `serial.cpp`는 read/write request를 처리한다. `v04_t13_twis_delay.py`의2ms 지연은 write_request만 표적으로 삼는다 | read_request 공급 생략·SCL LOW 관측·새 read/write 복구의 T13 전용 주입은 미구현 |
| GPIO alias·DMA 겹침 | `v04_t13_conflict.py`가 UART21/22의 alias·DMA, UART30의 같은 block 거부를 구분한다. 사용자 수용한14조건 결과 유지 | 해당 serial 결과를 모든 GPIO·stream의 active 충돌 PASS로 확대하지 않음 |
| GPIOTE/DPPI 채널·domain |95번에 실제 DPPI/EGU·group 예약 거부와 반환이 있다. `r07_event_driver_main.cpp`와 `m25_event_fabric_contract`에 중복 endpoint·cross-domain 거부가 있다 |95번은 시험용 예약, R07은 Host, contract는 target 코드다. 현재 active GPIOTE/DPPI 출력 보존을 동반한 T13 반복 증거와는 범위가 다름 |
| PWM과 analogWrite/tone/Servo 중복 | `ac02b_analog_contract`는 fake PWM backend로 사용자 API의 소유권·rollback을 검증한다.94번은 PWM 지연 시작 취소의 실기다 | fabric PWM 활성 상태에서 각 사용자 API 충돌과 실제 출력·buffer 보존을 확인한 T13 실기와 같지 않음 |
| System OFF 복구 | 전용 A 중계/B reset·timer/GPIO wake runner와 target이 있고 이번 bridge 최초 오류 원본 확보 | bridge 실패로 실제 OFF0회. 새 유휴선 보완 검증과 timer/GPIO wake가 남음 |

이 대조만으로 T13 전체 완료를 선언하지 않는다. 위 추가 계측은 기존 S 신호의 역할과 내부
자원으로 설계할 수 있으며 현재 단계에서 U 재배치를 요구할 근거는 아니다. UART00 외부
통신은 앞 표의 순서4에서 별도로 U 결선을 안내한다.

UART20 A CTS의 [예행](evidence/t13-s123-uart20-a-flow-preflight-5c4c3e0/manifest.json)과
[100회 원본](evidence/t13-s123-uart20-a-flow-100-5c4c3e0/manifest.json),
I2S 정상 재시작 실패의 [RAM·독립 분석](evidence/t13-s123-i2s-normal-failure-afb5431/manifest.json)을 보존했다.

## 9일00:34 KST — 독립 CTS 결과와 I2S 계측 준비

UART21 A CTS는 예행을 통과한 뒤 정식7회 성공·8회차 실패였다.
[실패 원본](evidence/t13-s123-uart21-a-flow-failure-5c4c3e0/manifest.json)의 B 수신 frame23,
offset852에서 기대229·실제120을 기록했다. UART22 A CTS는 예행 후56회 성공·57회차 실패이며
[양쪽 error event5/mask12](evidence/t13-s123-uart22-a-flow-failure-5c4c3e0/manifest.json)를
기록했다. 후자는 framing/break이고 최초 payload 불일치 기록은 없다. 둘을 같은 원인으로 확정하지 않는다.
두 실행의 PSEL 검사 실패는 오류 뒤 자동 정지·핀 반환 이후의 표면 오류다. 실제 lease 만료는0이며
양쪽 STOP·clock0·최종 입력 반환은 확인했다. UART30 이후의 독립 조건은 계속 실행한다.

System OFF 유휴선 보완 source는 `da89d2af8e4f77967dfc411a10afdd4bb2fecbb8`이다.
clean exact source의 power·일반 S 각 두 역할 빌드, power Host8·문서·계약·정렬을 통과했다.
원격 Host110그룹831시험도 통과했다. 이 software 결과는 bridge/OFF 실기 성공이 아니다.

I2S의 다음 계측은 opcode118 page17에 현재 ENABLE·MODE·RX/TX enable·DMA 주소·MAXCNT·
PSEL·P1.06/07 설정·입력 수준·IRQ priority/enable/pending을 읽는다. page18은 실행 상태·STOP
호출 횟수·반환/제출량·event·clock 핀 설정을 읽고 page19는 최초 RX 불일치 직후, 자동 STOP 전의
page17 값을 보존한다. 긴 RX 원본 조회보다 이 상태를 먼저 읽는다. 레지스터는 읽기만 하며 event를
지우거나 GPIO·IRQ 설정을 바꾸지 않는다. 정상 데이터 오류 판정과 공급 중단 허용 범위는 유지한다.
개발 image 두 역할 빌드·관련 Host는 통과했으며 새 exact image의 실기는 아직 미실행이다.

## 9일00:46 KST — 계측 결과와 System OFF 구간 분리

- UART30 A CTS도 [100회](evidence/t13-s123-uart30-a-flow-100-5c4c3e0/manifest.json)를 통과했다.
  양쪽 cleanup203그룹·입력 반환을 확인했다. 기존54조건 중 확정 통과는 UART20·30의2조건이다.
- System OFF [새 bridge](evidence/t13-s123-power-idle-bridge-da89d2a/manifest.json)는 opcode131
  응답 timeout으로 실패했다. A의 RX는 내부 pull-up 전0·후1이고 첫 UART 오류 기록은 없었다.
  B의 cleanup SWD와 별도 read-only 재확인은 No ACK였다. 추가 pin reset1회 뒤에야 B SWD가
  회복됐고 양쪽17핀 입력·STOP stamp를 확인했다. 이 reset은 실패 후 정리이며 정상 bridge나
  OFF 성공이 아니다. [정리 원본·첫 reset helper의 실행 전 표기 오류 정정](evidence/t13-s123-power-idle-cleanup-da89d2a/manifest.json)을
  함께 보존했다. B 최초 실패 시점의 RAM은 읽지 못했으므로 원인을 확정하지 않는다.
- I2S source182ef13의 [예행](evidence/t13-s123-i2s-registers-preflight-182ef13/manifest.json)은
  통과했으며 [100회 실행](evidence/t13-s123-i2s-registers-failure-182ef13/manifest.json)은5회
  성공 후6회차 정상 구간에서 실패했다. 측정 후 quiesce·drain 중 A index158569부터13word가
  불일치했다.0이 연속된 뒤1이 연속되고 같은 반환 버퍼 안에서 정상 패턴으로 돌아온 원본이다.
  A 최초 오류 snapshot은 ENABLE1·RX/TX1·PSEL36/37/38/39·IRQ priority7/enable1/pending0이었다.
  B는 이후 조회에도 ENABLE1·같은 정상 PSEL·STOP 호출0·RX 불일치0이었다. 따라서 이 실행에서
  B의 명시적 STOP 때문에 잘린 것으로 판정할 수 없다. 양쪽 TX3slot은 원래 seed와 같고
  양쪽 종료·clock0·핀 반환도 확인했다. [RAM·독립 분석](evidence/t13-s123-i2s-registers-analysis-182ef13/manifest.json)을 보존했다.

다음 power 진단은 `bridge-debug`1회다. 같은 UART·GPIO·DMA에서 B debug를 유지해 통신 자체와
debug 분리/pin reset 경로를 구분한다. Host9시험을 통과했으며 실기는 아직 미실행이다.
이 진단은 OFF를 요청하지 않으며 성공해도 정상 mode·timer/GPIO 복구의 PASS로 세지 않는다.
I2S 실패 원본 수집 후 기존54조건 실행을 재개했다. 연속 handover·완료된 정상36조건·면제한
자원 충돌900회를 다시 예약하지 않는다.

## 9일00:58 KST — 중계 RX 재준비 실패와 C01 원본

- [C01 A CTS](evidence/t13-s123-c01-a-flow-failure-5c4c3e0/manifest.json)는60회 성공 뒤
  61회차에 실패했다. CTS 대상은 UART30이지만 최초 payload 오류는 A의 배경 TWIM22 수신
  frame26/offset225, 기대5·실제31이었다. B UART30에도 framing mask4가 남았다. 이 관측만으로
  두 오류의 선후 인과를 확정하지 않는다. 양쪽 STOP·clock0·최종17핀 입력을 확인했다.
  기존54조건의 확정 통과는2조건이며 C05 이후 독립 실행을 계속한다.
- source `f4715344e40c128fdb0b265a949cf3f42617d0a5`의 power/일반 두 역할 빌드·Host9·
  문서·계약·정렬과 원격 Host110그룹832시험이 통과했다. [debug 유지 bridge](evidence/t13-s123-power-debug-bridge-f471534/manifest.json)도
  첫131 응답 timeout으로 실패했다. reset/OFF를 요청하지 않았으며 양쪽 STOP·입력을 확인했다.
- [종료 후 읽기 전용 RAM](evidence/t13-s123-power-debug-ram-f471534/manifest.json)은
  A TX1/RX0/error0, B TX0/RX1/error11이었다. B는 요청을 받았지만 다음 RX 준비에서 실패한 뒤
  응답 TX를 정지했다. 양쪽 최초 UART event 오류는 없었다. 고정 SDK의 nrfx는 RX_DONE 후
  STOPRX를 처리하고 별도 RX_DISABLED에서 core의 rx_active를 해제한다. 따라서 즉시 재준비
  시점이 직접적인 후보이며, 아직 수정 후 실기로 확정하지 않았다.
- 다음 exact image는 전용 power 응답 전2ms 간격을 두고 RX 준비 후 TX를 시작한다.
  재준비 거부의 SerialFabricResult를 별도 오류에 남기며 자동 재시도하지 않는다. 정상 오류·
  nonce·sequence·OFF reset/retention 판정은 유지한다. debug 진단 → 정상 bridge → 가능한
  timer/GPIO 순서로 검증하며 현재 실제 OFF 성공은0회다.

이번 남은1~3번 진행률의 분모는 I2S1 + 기존 S54 + timer/GPIO2 + 요구 대조1 =58항목이다.
현재 확정 완료는 UART20/30 A CTS2 + 요구 대조1 =3/58(약5%)이며 문서 정리 완료와 별도다.
실패 진단·예행·부분 반복을100회 조건의 통과로 올리지 않는다.

## 9일01:08 KST — RX 응답 순서 보완 통과와 정상 mode의 미수신

- [C05 A CTS](evidence/t13-s123-c05-a-flow-failure-5c4c3e0/manifest.json)는46회 성공 뒤
  47회차 실패였다. B UART30 수신 frame80/offset0은 기대62·실제167이었고 A TWIM22에도
  error42/detail2가 남았다. 이 관측을 동일 원인으로 확정하지 않는다. 양쪽 STOP·clock0·
  최종 입력 반환을 확인했고 UART20 B부터 계속한다.54조건 확정 통과는 여전히2조건이다.
- source `e791a589aed26faf0cc3f646c73beb373141fb3d`의 power·일반 S 각 두 역할 빌드와
  Host9·문서·계약·정렬, 원격 Host110그룹832시험을 통과했다.
  [응답 순서 보완 후 debug 진단](evidence/t13-s123-power-turnaround-debug-e791a58/manifest.json)은
  source/nonce/sequence·새20word·양쪽 STOP을 통과했다. 이는 앞선 RX 재준비 실패의 보완
  결과이며 정상 debug-free 또는 OFF 성공으로 확대하지 않는다.
- [정상 bridge](evidence/t13-s123-power-turnaround-bridge-e791a58/manifest.json)는 여전히
  최초 응답 timeout이었다. 이번에는 B SWD 재접속과 양쪽 정리를 확인했다.
  [종료 후 RAM](evidence/t13-s123-power-turnaround-ram-e791a58/manifest.json)은 A TX1/RX0,
  B TX0/RX0·error40(lease 만료)이었다. B는 pin reset·retention을 처리했지만 UART 요청을
  받지 못했다. 첫 UART 오류 기록도 없으므로 앞선 error11과 같은 실패로 쓰지 않는다.
- 전용 power image의 초기 XO.STAT는 양쪽0이었다. [Nordic UARTE 설명](https://docs.nordicsemi.com/r/bundle/ps_nrf54l15/page/uarte.html)은
  안정된 통신에 외부 크리스털 활성화를 요구한다. 일반 T13은 이미 참조를 획득하지만 전용
  power 경로에는 빠져 있었다. 다음 보완은 UART 전 참조 획득, DMA STOP 후 참조 반환,
  OFF 전 XO 정지 확인, reset/wake 뒤 재획득이다. 이 누락과 미수신의 인과는 새 실기로 확인한다.

현재 I2S B·실패한 CTS 조건·System OFF는 미완료이며 timer/GPIO 실제 OFF는0회다.

## 9일01:24 KST — UART20 B와 HFXO 보완 결과

- [UART20 B CTS](evidence/t13-s123-uart20-b-flow-failure-5c4c3e0/manifest.json)는68회 성공 뒤
  69회차의 CTS 주입 전 정상 begin 구간에서 실패했다. A 수신 frame0/offset0은 기대103·
  실제20이었고 B에는 framing/break mask12가 남았다. CTS LOW 유지가 직접 원인이라고
  단정하지 않는다. 양쪽 cleanup140그룹·clock0·최종17핀 입력을 확인했다. UART21 B부터
  독립 조건을 계속한다. 확정 완료는 여전히3/58이며 부분 반복은 조건 PASS가 아니다.
- source `03257b6b84f776fac6640c6ba8d751237f450c45`의 power·일반 각 두 역할 target,
  power Host10·문서·계약·정렬을 통과했다. 원격 Host110그룹833시험 PASS,01:20 KST에
  원격15검사 중8성공·7진행 중·실패0이었다. 진행 중 검사를 통과로 기록하지 않는다.
- [HFXO 보완 정상 bridge](evidence/t13-s-power-clock-bridge-failure-03257b6/manifest.json)는
  최초131 응답 timeout으로 실패했다. 초기 XO.STAT는 양쪽65537로 실제 실행 중이었고,
  B 초기 RX는 pull-up 후에도 LOW였다. 첫 오류 mask0x80000000은 UarteFabric의 합성
  이벤트 큐 초과 표시이며 하드웨어 framing 값 자체가 아니다.
  [읽기 전용 RAM](evidence/t13-s-power-clock-ram-03257b6/manifest.json)은 A TX1/RX0/error0,
  B TX0/RX0/error46이었다. 양쪽 STOP·clock 보유0·17핀 입력은 확인했다.
- 다음 계측은134의 별도 page에서 A의 peer reset 전후·RX 시작 뒤 OUT/IN/PIN_CNF·
  UARTE ENABLE/PSEL·clock을 읽는다. B는 debug 분리 뒤 cleanup 전까지 SWD로 읽지 않는다.
  기존 최초 오류를 유지하면서 별도80byte에 처음 전달된 저위4bit UART 오류도 보존한다.
  RX 이벤트를 버리거나 큐를 늘리지 않고, 통신·OFF 성공 기준과 기존 실패는 유지한다.

이번 보완은 System OFF 선행 중계의 진단이다. 정상 CTS/I2S 실패의 원인을 해결한 것으로
확대하지 않는다. Nordic GPIO100은 power-on reset 조건의 현상이며 이번 runtime 데이터
오류나 B RX LOW의 원인으로 확정할 근거가 없다.

## 9일01:38 KST — UART21 B 통과와 중계 실패 후 정리

- [UART21 B CTS100회](evidence/t13-s123-uart21-b-flow-100-5c4c3e0/manifest.json)를 통과했다.
  양쪽 cleanup203그룹·idle400기록·17핀 입력 반환을 확인했다. 기존54조건 중 확정 통과는
  UART20/30 A와 UART21 B의3조건이다. 요구 대조를 더한 이번 순서1~3 완료는4/58(약7%)다.
- source `28cf8804d80193d16822fdc61cfbdba817924c5b`의 power·일반 S 각 두 역할 target,
  power Host12·문서·계약·정렬과 원격 Host110그룹835시험, software7검사를 통과했다.
  직전03257b6의 원격 상태는01:30 KST에8성공·후속 push로7취소였다.15성공으로 쓰지 않는다.
- [새 유휴선 bridge](evidence/t13-s-power-pins-bridge-failure-28cf880/manifest.json)는 첫131
  응답 timeout이었다. A의 peer reset 전후·RX 시작 뒤·실패 시 TX/RX는 모두 HIGH,
  GPIO/PSEL도 선언값이었다. B의 최초 오류와 부팅 상태는 cleanup No ACK로 읽지 못했다.
  A의 몇 시점 HIGH 관측을 B의 부팅 중 전 구간 HIGH나 결선 파형의 정상 증명으로 확대하지 않는다.
- [실패 후 정리 기록](evidence/t13-s-power-pins-cleanup-28cf880/manifest.json): B pin reset1회
  뒤에도 No ACK였다. reset 유지 접속은 회복됐지만 첫 재개 후 identity는0이었다. 읽기 전용
  확인에서 B는 HALTED, PC0xa638의 GRTC 초기화 코드에 있었고17핀은 입력이었다.
  실제 HALTED와256byte 부팅 코드 일치를 확인한 다음 재개하자 정상 source/role·STOP stamp·
  17핀 입력을 확인했다. 재개 전 GRTC high BUSY는0이고 counter가 유효했으므로 이 PC 위치만으로
  SDK GRTC 무한 대기를 확정하지 않는다. 첫 under-reset 뒤 재개가 충분하지 않았던 경위도
  남긴다. flash·unlock·mass erase는 수행하지 않았다.
- B 최초 실패 RAM은 reset으로 소실됐으며 원인은 미확정이다. System OFF0회 상태를 유지하고,
  양쪽 종료 증명 후 UART22 B 본 검증부터 기존 S 묶음을 재개했다. 사용자의 결선 확인 시간은
  갱신하거나 연장하지 않았다.

## 9일01:48 KST — UART22 B 통과와 CTS 시작 준비 보완

- [UART22 B100회](evidence/t13-s123-uart22-b-flow-100-5c4c3e0/manifest.json)와 양쪽 STOP·
  clock0·입력 반환을 확인했다. UART30 B를 실행 중이며 현재 확정 완료는5/58(약9%)다.
- UART20 B69회차의 [첫4byte 독립 대조](evidence/t13-s-uart20-b-start-prefix-5c4c3e0/manifest.json)에서
  A의 첫 RX `14 04 0c 64`는 같은 seed의 초기8192byte 중 offset2048, 즉 송신 frame2 시작에만
  일치했다. 전체 RX frame을 보존한 결과는 아니며, 이4byte만으로 누락 원인을 확정하지 않는다.
  당시 실패는 CTS 주입 전이고 A TX0/RX0·B TX3/RX0이었다.
- 기존 시험의 `serialStart()`는 각 보드의 RX를 준비하고 해당 보드 기준100ms 뒤 송신했다.
  PC가 두 번째98을 늦게 전달하면 먼저 시작한 보드가 peer RX 준비 전에 송신할 수 있다.
  따라서 고정 CTS 유지/정상 재획득 구간에 한해98 인수1로 양쪽 RX를 준비한 뒤184로 송신을
  허용하도록 보완했다. 새 시작·허용 응답의 Host 상대 시각도 기록한다. 코어 UART 구현이나
  payload/CTS 오류 허용 범위는 바꾸지 않았다.
- Host의250ms 시작 간격·RX 준비 실패 시 송신 불허·원래 시작 경로·범위 거부를 검사했다.
  관련 T13 Host108시험은 통과했다. target·exact image·UART20 B 재실기는 아직 미실행이다.
  UART21/22 A 및 C01/C05의 동작 중 실패까지 같은 원인으로 단정하지 않는다.

## 9일01:56 KST — UART30 B 통과와 수정 source 고정

- [UART30 B CTS100회](evidence/t13-s123-uart30-b-flow-100-5c4c3e0/manifest.json)가 통과했다.
  양쪽 cleanup203그룹·idle400기록·17핀 입력 반환까지 확인했다. 기존54조건은5조건 통과,
  순서1~3은 요구 대조1건을 더한6/58(약10%)다. C01 B를 이어서 실행한다.
- RX 준비 절차를 `1a00a8323e0368ca623de7a717009a4087b1ab23`로 커밋·푸시했다.
  해당 clean source의 T13 Host108·문서·계약·정렬은 통과했다. target과 원격 Host를 확인한
  다음 기존 UART20 B 실패 조건을 재검증한다. 아직 이 수정의 물리 PASS는 없다.
- 직전 문서 source73f812e의 원격 Host는110그룹835시험을 통과했다.01:53 KST 관측의
  전체 검사는8성공·7진행 중이었으므로15검사 전부 성공으로 기록하지 않는다.

## 9일02:01 KST — C01 B 실패와 UART20 B 재검증

- [C01 B CTS 원본](evidence/t13-s123-c01-b-flow-failure-5c4c3e0/manifest.json)은74회 완료 뒤
  75회차 정상 재획득 구간에서 실패했다. 표면 오류는 lease renewal이지만 실제 만료 수는
  양쪽0이었다. A의 UART21에서 error25/mask4 framing, B의 TWIS22에서 error3/detail171을
  관측했다. 어느 오류가 먼저였는지는 확정하지 않으며 payload 최초 오류는 없었다.
  양쪽 cleanup153그룹·idle300기록·17핀 입력 복귀를 확인했다.
- UART20 B 재검증 source1a00a832의 [두 역할 target과 원격 Host111그룹838시험](evidence/t13-start-barrier-gates-1a00a83/manifest.json),
  software7검사가 통과했다. 전체15검사 중8개는 관측 시점에 진행 중이었다.
  수정 image의 예행은 성공했고100회 본 시험을 실행 중이다. 다른 CTS 조건을 통과로 바꾸지 않았다.
- 순서 예약기가 기존 실행기와 같은 감사 파일을 동시에 쓰려다 exclusive 생성 거부로
  대기했다. 기존 실행기가 먼저 저장한 STOP 증거를 확인하고 예약기만 수정·재개했다.
  실기 오류를 재시도한 것이 아니며 진행 중인 C01을 중단하거나 증거를 덮어쓰지 않았다.
- power 중계의 debug 유지/해제 차이를 좁히기 위해 단일 `bridge-fast-poll`을 준비한다.
  정상 pin reset과 UART 조건은 유지하고 main의1ms sleep만10us polling으로 비교한다.
  정책을 reset 전후 응답으로 확인하며 이 정책에서는 firmware가 OFF 명령을 거부한다.
  이 진단의 성공은 정상 polling·System OFF PASS가 아니다. 아직 target·실기는 미실행이다.

## 9일02:08 KST — 시작 준비 보완의 한계 확인

- source1a00a832의 [UART20 B 예행](evidence/t13-s-uart20-b-start-barrier-preflight-1a00a83/manifest.json)은
  성공했지만 [100회 시험](evidence/t13-s-uart20-b-start-barrier-failure-1a00a83/manifest.json)은
  61회 완료 뒤62회차 maintained 구간에서 실패했다. A는 error25/mask4 framing,
  B는 error25/mask12 framing+break, payload 최초 오류는 없었다. A TX26/RX22·B TX23/RX26으로,
  기존69회차의 첫 RX frame 시작 누락과는 다른 동작 중 실패다. 오류의 최초 발생 순서는 미확정이다.
- 양쪽 lease 만료0, cleanup126그룹·idle246기록·17핀 입력 반환을 확인했다. 시작 장벽의
  순서 보완이 전체 CTS 실패를 해결했다고 하지 않으며 실제 통과 항목은 추가하지 않았다.
  기존 S54를 C05 B부터 이어갔다. 현재 순서1~3 확정 완료는6/58(약10%)다.

## 9일02:17 KST — parity 주입 판정 교정과 남은42조건 준비

- [C05 B CTS](evidence/t13-s123-c05-b-flow-failure-5c4c3e0/manifest.json)는11회 완료 뒤12회차
  maintained 구간에서 실패했다. A UART30은 error25/mask4, B TWIS22는 error3/detail81이었다.
  최초 payload 오류는 없었고 lease 만료는 양쪽0이다. cleanup26그룹·idle46기록·17핀 반환을
  확인했다. 실제 오류의 최초 발생 순서와 원인은 미확정이다.
- parity A 예행은 [UART20](evidence/t13-s123-uart20-a-parity-oracle-5c4c3e0/manifest.json),
  [UART21](evidence/t13-s123-uart21-a-parity-oracle-5c4c3e0/manifest.json),
  [UART22](evidence/t13-s123-uart22-a-parity-oracle-5c4c3e0/manifest.json),
  [UART30](evidence/t13-s123-uart30-a-parity-oracle-5c4c3e0/manifest.json) 모두 mask6이었다.
  8N1의 stop1을8E1 parity로 읽고, 다음 start0을 stop으로 읽으면 parity와 framing이 함께
  발생할 수 있다. 기존 mask2 단독 판정이 이 주입 방식을 충분히 반영하지 못했다.
- parity bit를 필수로 하고 mask2/6만 허용한다. overrun·break·unknown bit와 framing 단독은
  거부한다. API/raw mask 일치·guard·STOP 및 새 seed 정상 재시작 기준은 유지한다. wire bit를
  독립 구성한 Host 시험과 잘못된 mask·STOP 거부를 추가했고 해당9시험은 통과했다.
  기존 실패는 사후 PASS로 바꾸지 않으며 새 source의 예행과100회 완료가 필요하다.
- [기존54 대열](evidence/t13-s123-queue-checkpoint-5c4c3e0/manifest.json)은 CTS12와 parity A4의
  종료 증거를 보존했다. parity B 첫 실행은 우선순위 대기 중이었고 아직 보드를 조작하지 않았다.
  양쪽 STOP·17핀 입력·hardware inactive를 확인한 경계에서 대기 예약기만 교체했다.
  다음 대열은 parity8·break8·RX 지연8·SPI10·TWI4·TWIS 지연4의42조건이다.
- power source902a1da의 [두 power/두 일반 target·Host110·문서·계약·정렬](evidence/t13-power-poll-gates-902a1da/manifest.json),
  원격 Host111그룹840시험과 software7검사는 통과했다. 관측 시점의 전체15개 중8개는 진행 중이었다.
  [fast polling 중계 비교](evidence/t13-s-power-poll-bridge-failure-902a1da/manifest.json)는 첫131
  timeout으로 실패했다. B 부팅7ms에 RX LOW·TX HIGH, 최초 hardware framing mask4를 기록했다.
  A의 reset 전후 몇 시점 TX HIGH만으로 그 사이 B RX의 실제 파형을 증명할 수 없다.
  양쪽 STOP·clock 반환·17핀 입력을 확인했으며 OFF는 실행하지 않았다.

현재 순서1~3 확정 완료는6/58(약10%)이다. 정상 CTS/I2S 오류를 parity 판정 교정으로 해결했다고
하지 않는다. 결선 확인 유효기간·10MHz·exact UID·controlled flash 조건도 그대로 유지한다.

## 9일02:25 KST — 주입 peer RX와 power 시작 순서 보완

- source b6ac3bef의 [exact 두 역할 target·Host113·문서·계약·정렬과 원격 Host843](evidence/t13-parity-gates-b6ac3be/manifest.json)을
  통과했다. 원격 software7검사는 성공했으며 이후 target CI 전체 성공으로 확대하지 않는다.
  수정 판정의 남은42조건을 재개했다. [UART20 A 예행](evidence/t13-parity-peer-rx-failure-b6ac3be/manifest.json)은
  DUT event5/mask6을 확인했으나 peer lane도 error25/mask12여서 실패했다. 양쪽 종료는 확인했다.
- DUT는 송신하지 않는데 주입 peer에서 일반 양방향 RX를 열고 있던 시험 구성을 확인했다.
  DUT 오류·자동 STOP 뒤 선 반환이 이 수신 오류를 만들었는지는 아직 미확정이다.
  주입 구간의 peer를 송신 전용으로 보완하고 RXSTARTED와 DMA pending0·계측 marker를 필수로
  기록한다. peer 오류를 무시하지 않으며 정상 baseline·새 seed 재시작은 양방향 기준을 유지한다.
- power902a1da 원본에서 B reset 전후 A UART ENABLE은0, A RX 시작 뒤8이었다. B의 최초
  RX LOW와 framing은 부팅7ms였다. B reset 전에 양쪽 핀 준비·A RX 시작·ENABLE8·PSEL·
  오류/트래픽0을 증명하도록 순서를 바꾼다. A ENABLE0이 원인이라고 확정한 것은 아니며
  동일115200·원래 polling·정상 debug 분리 bridge로 재검증한다.
- 두 보완의 관련 Host116은 통과했다. 새 target·실기는 아직 미실행이며 진행률에 추가하지 않는다.
  현재 고정 b6ac3bef 대열은 독립 조건을 계속 수행하고 기존 실패를 보존한다.

## 9일02:51 KST — 보완 실기 결과와 첫 오류 감시점 준비

- source7d080ed2의 [일반 두 역할·power 두 역할 target, Host116·문서·계약·정렬](evidence/t13-peer-rx-gates-7d080ed/manifest.json)은
  통과했다. 원격 Host111그룹846시험과 software7검사는 성공했다.02:47 관측 전체 CI는9/15 성공·6진행 중·실패0이며 전체 성공으로 확대하지 않는다.
- b6ac3bef의 [parity8 예행 요약](evidence/t13-parity-peer-failures-b6ac3be/manifest.json)은 모두 peer RX 오류로 실패했다.
  이후 송신 전용 peer 보완 source7d080ed2의 [UART20 A 예행](evidence/t13-peer-rx-parity-uart20-a-preflight-7d080ed/manifest.json)은
  통과했다. [100회 본 시험](evidence/t13-peer-rx-parity-uart20-a-failure-7d080ed/manifest.json)은21회 완료 뒤22회차 **정상 재시작**에서
  A error25/mask14·B error25/mask12로 실패했다. 양쪽 lease 만료0·cleanup69그룹·idle132·17핀 반환을 확인했다.
  정상 구간 A parity bit의 출처가 과거 ERRORSRC인지 새 오류인지는 아직 확정하지 않는다.
- b6ac3bef [UART20 A break](evidence/t13-s-break-uart20-a-failure-b6ac3be/manifest.json)는60회 뒤61회차 **정상 baseline**에서
  양쪽 error25/mask12로 실패했다. TX/RX9frame·payload 최초 오류 없음·lease 만료0이며 종료 원본을 보존했다.
  UART21 A break 예행은 통과했고 현재100회를 수행 중이다. 서로 다른 구간의 실패를 모두 오류 주입 자체의 실패로 설명하지 않는다.
- [A 준비 후 정상 polling 중계](evidence/t13-power-ready-bridge-failure-7d080ed/manifest.json)와
  [빠른 polling 중계](evidence/t13-power-fast-ready-bridge-failure-7d080ed/manifest.json)는 모두 최초131응답 timeout이다.
  정상 polling에서 B 초기 RX HIGH·최초 hardware fault 없음·10초 lease 만료를 기록했지만 중계는 성립하지 않았다.
  빠른 비교의 B cleanup No ACK는 [후속 정리](evidence/t13-power-fast-ready-cleanup-7d080ed/manifest.json)에서
  pin reset·reset 유지 접속 후 HALTED·부팅256byte 일치·PC0xa7d8을 확인하고 resume해 복구했다.
  양쪽 stamp0x53540004·17핀 입력이다. flash/unlock은 없었으며 최초 실패의 B RAM은 reset으로 소실됐다.
  이는 System OFF 성공이나 GRTC 원인 확정이 아니다. OFF 명령은 실행하지 않았다.
- 중계 진단 때문에 시작하지 못한 UART21 A parity placeholder를 NOT RUN으로 정정했다.
  원래 controller JSON도 별도 보존했다. 소비한 중계 hook을 제거하고 STOP 증거 뒤 독립 S 대열을 재개했다.
- 사용자가 제안한 중단점 디버깅을 보강한다. 기존 수집은 오류를 저장한 펌웨어에 SWD mailbox로
  조회하며 자동 STOP 뒤 상태가 섞였다. 이번에는 두 역할의 exact ELF/DWARF에서 `Lane.error`를 찾아
  정상 baseline/restart에만 DWT write watchpoint를 설치한다. 오류 기록 직후 양쪽을 정지하고
  CPU·SCB/NVIC·UART 설정/ERRORSRC/DMA·GPIO·클록·lane/event queue·stack을 저장한 뒤 해제·resume·STOP을 확인한다.
  양쪽 정지는 동시가 아니며 NRFX가 IRQ에서 이미 소비한 ERRORSRC는0일 수 있다. 저장한 event mask와
  정상 시작 전 설정을 함께 비교한다. 멈춤이 만든 후속 peer 오류를 최초 원인으로 확정하지 않는다.
  제품 firmware는 변경하지 않으며 정상 조건의 첫 오류 분석용이다. 새로운 기능 조건이나 물리 PASS가 아니다.
  [진단 도구 원본과 사전 검사](evidence/t13-uart-watch-preparation-7d080ed/manifest.json)에
  SVD·exact ELF·감시점 설치/해제6검사와 적중/읽기 실패 시 양쪽 재개2개 Host 시나리오를 보존한다.

현재 순서1~3의 확정 완료는6/58(10.3%)이다. 결선·UID·10MHz·배타 lock·controlled flash 조건은 유지한다.

## 9일03:03 KST — 실제 감시점 적중과 UART 버퍼 대조

- b6ac3bef의 UART21 A break [예행](evidence/t13-break-uart21-a-preflight-b6ac3be/manifest.json)과
  [100회](evidence/t13-break-uart21-a-pass-b6ac3be/manifest.json)를 통과했고 양쪽 STOP·핀 반환을 확인했다.
  확정 완료는7/58(12.1%)다. [UART22 A 예행](evidence/t13-break-uart22-a-failure-b6ac3be/manifest.json)은
  정상 baseline에서 B RX frame26 offset0의1bit 차이, A mask12로 실패했다. 최초 순서는 미확정이며 정리 뒤 독립 대열을 이어갔다.
- [exact7d080ed2 감시점 진단](evidence/t13-uart20-first-error-watch-7d080ed/manifest.json)은13회를 관측한 뒤
  14회차 정상 baseline에서 B `Lane.error` 쓰기에 적중했다. B DFSR4(DWTTRAP)·PC0x2320,
  error6(payload)이며 아직 active1이었다. A는 실행 중·오류0이었다. A를 이어서 halt했고 전체 읽기·resume은0.453초였다.
- [독립 RAM 분석](evidence/t13-uart20-watch-analysis-7d080ed/manifest.json)에서 B RX slot0은 예상26 대신27,
  slot1은28 frame과 각각1024byte 전부 일치했다. A의 TX RAM은28/29 frame과 일치했다.
  이는 수신에서 한 frame이 빠진 관측이며, 최초 송신 누락인지 DMA overwrite인지 완료 처리 누락인지는 아직 구분하지 못했다.
- 양쪽 UART ENABLE8·CONFIG4097(parity 제외, HWFC)·1Mbps였다. 새 ERROR event0, DMA BUSERROR0,
  API event queue overflow0, HardFault/CFSR0, 관측 순간 BASEPRI/PRIMASK0, XO/PLL 실행 상태였다.
  A ERRORSRC2·B12는 정상 시작 전에도 있던 값으로 적중 시점에 그대로였다. 이 값만 보고 새 오류나 IRQ 우선순위를 원인으로 단정하지 않는다.
  양쪽 halt는 동시가 아니고 peripheral은 멈춤 동안 계속 동작하므로 peer의 후속 상태는 그 한계를 포함한다.
- 감시점을 제거하고 두 보드를 resume한 뒤 cleanup43그룹·idle80기록·17핀 입력 복귀를 확인했다.
  새 결선 검사105/105도 통과했다. 진단의 정상 부분 반복은100회 복구 PASS에 더하지 않았고 독립 S 대열을 재개했다.
- 다음은 기본 비활성 `CONFIG_NUCODE_T13_UART_TRACE`다. 진단 image의 link wrapper가 원래 nrfx
  init/RX buffer 등록/RX enable·callback과 context를 보존하면서64건×24word 이력을 기록한다.
  RX_DONE·TX_DONE callback 순간의 첫 word, 실제 PTR/AMOUNT/READY/END와 driver current/next buffer를
  이후 DMA RAM과 대조한다. 이력 저장은 짧게 IRQ를 잠그므로 시간에 영향을 줄 수 있는 진단이며 새 안정성 PASS가 아니다.
  관련 Host119은 통과했고 trace를 켠 새 target·실기는 아직 미실행이다. SDK·제품 adapter·공개 API는 수정하지 않았다.

Nordic의 [과거 ENDRX/RXSTARTED 순서 사례](https://devzone.nordicsemi.com/f/nordic-q-a/84528/uarte-missed-isr-endrx-or-generated-excess-isr-rxstarted)는
nRF52833·NCS1.6 Zephyr async 경로이며 이 보드의 원인 증거가 아니다. 현재 고정 SDK의 raw nrfx
callback·RX pointer 전환을 실제 이력으로 대조한다. FRAMETIMEOUT도 이번 양쪽 관측에서0이었다.

## 2026-09-09 03:12 KST — UART30 A 완료와 net3 재검사

이번1~3단계의 확정 완료는 **8/58(13.8%)**다. CTS5조건·break UART21/30 A2조건과
기존 요구 대조1건이며, I2S와 System OFF는 미해결이다. GPIO 진단을 복구 PASS로 더하지 않는다.

- source b6ac3bef의 UART30 A break는 예행 뒤100/100을 통과했다.
  [예행 원본](evidence/t13-break-uart30-a-preflight-b6ac3be/manifest.json),
  [100회 원본](evidence/t13-break-uart30-a-pass-b6ac3be/manifest.json).
- 다음 UART20 B 시험 전에 net3 **A P1.04→B P1.05**의 LOW 입력이 HIGH였다.
  A 출력LOW·양쪽 pulse/lease 만료0였고, 양쪽 결선 STOP의 방향·pull·소유권은0이었다.
  이 조건의 통신 복구는 시작하지 않았으므로 NOT RUN이다. 기존 대열의 generic cleanup 오류는
  통신 시작 전에는 없는 T13 최종 pin record를 요구한 감사 결과이며 결선 STOP 실패를 뜻하지 않는다.
  [첫 실패 원본](evidence/t13-net3-wiring-failure-b6ac3be/manifest.json).
- 물리 변경 없이 동일 선만 양방향10회씩20pulse를 재검사했다. LOW 뒤5/50/150ms와 해제 값을
  읽었고, GPIO IN·OUT·DIR·PIN_CNF 직접 관측을 포함해 불일치0이었다. 양쪽17핀 CNF0을 확인했다.
  [한 선 진단·대열 원본](evidence/t13-net3-isolated-review-b6ac3be/manifest.json).
- 이어 원래 전체17신호 절차105/105와 양쪽 STOP3묶음이 통과했다.
  [전체 재검사](evidence/t13-net3-full-wiring-review-b6ac3be/manifest.json).
  재현되지 않은 최초 실패를 삭제하거나 점퍼 불량·UART 원인으로 단정하지 않는다.
  이 선은 UART A TX/B RX와 겹치므로 다음 진단에서도 관측 대상으로 유지한다.
- 미실행 S30조건만 별도 대열로 재개한다. 이미 실패한 동일 source 조건과 완료한 정상 soak,
  사용자 제외 handover·수용된 충돌 반복을 다시 예약하지 않았다.

## 2026-09-09 06:35 KST — UART 오류 정지 원인 수정과 line16조건 완료

이번1~3단계의 확정 완료는 **22/58(37.9%)**다. CTS5·parity8·break8과 기존 요구 대조1건이다.
부분 반복과 진단, 잘못 고른 build suite는 조건 PASS에 더하지 않았다. 기존 S54 중21조건이
완료됐고 CTS7·RX 공급 지연8·SPIS 경계10·TWI stuck-low4·TWIS 공급 지연4의33조건이 남았다.

- source8f6596d의 callback trace 진단에서 role1 event queue overflow와 error mask2/4/6을
  관측했다. 약509µs에 callback32건이 들어왔고, 오류가 난 RX를 계속 유지한 것이 첫 제품 원인이다.
  source16f298b는 첫 hardware error 뒤 async RX abort를 요청했다. 그러나 abort가 진행 중일 때
  lifecycle STOP이 `-EINPROGRESS`를 받은 뒤 소프트웨어 flag를 먼저 지워 UARTE ENABLE이 남았고
  정상 재시작이 wrong-state7로 실패했다. source46b7dc2는 pending abort를 기다린 뒤 상태를
  정리한다. 관련 UART/T13 Host·문서·계약·정렬 gate와 원격 Windows Host가 통과했다.
  trace ON CMSIS-DAP에서는 정리 뒤 active/RX/cancel0, UARTE ENABLE/INTEN/RXTO/SHORTS0,
  XO/PLL off와 핀 반환을 확인했다. [진단·gate·레지스터 원본](evidence/t13-uart-error-stop-diagnosis-46b7dc2/manifest.json).
- trace OFF source46b7dc2에서 UART20 A parity 예행·100회를 먼저 통과했다. 이어 이전에 실패했던
  UART20 A·UART22 A break와 UART20/21/22/30 B break, UART20 B·UART21 A/B·UART22 A/B·
  UART30 A parity를 각각 예행 뒤100/100 완료했다. source b6ac3bef의 기존 UART21/30 A break와
  합쳐 break8/8이며, 아래 UART30 B까지 합쳐 parity8/8이다. 각100회 감사값은 양쪽 cleanup303그룹,
  idle600기록, expected-fault200건과 최종17핀 반환이다. SDK Python/pyOCD 경로 문제로 보드 실행
  전 끝난 UART20 A break 예행2건도 삭제하지 않았다.
  [신규 break6·parity7 원본](evidence/t13-uart-break-parity-results-46b7dc2/manifest.json).
- source46b7dc2 UART30 B parity는89회 완료 뒤90회차 최초 오류가 mask14
  (parity+framing+break)여서 즉시 중단했다. 허용 mask2/6을14로 넓히지 않았다. CMSIS-DAP로
  cleanup 뒤 양쪽 UARTE30 ERRORSRC/ENABLE/INTEN/EVENTS_ERROR/RXTO0, XO/PLL off, PSEL disconnect와
  context active/TX/RX/cancel0을 확인했다. [nRF54L15 UARTE 정의](https://docs.nordicsemi.com/r/bundle/ps_nrf54l15/page/uarte.html-topic?contentId=IH1i5evkVp96RAHz1B~e5g)는
  RXD가 한 data frame보다 오래 active LOW일 때 BREAK를 세우고 reception 자체는 계속한다고 명시한다.
  8N1 peer의 정상 frame에는 stop HIGH가 있으므로 무작위 payload를 BREAK 허용 근거로 삼지 않았다.
- parity 러너가 DUT RX를 먼저 시작하고 그 뒤 peer TX를 시작해, PREPARE 뒤 아직 구동되지 않은 선을
  DUT가 잠시 관측할 수 있었다. source2e4ec73은 parity에서 peer TX를 먼저 시작하도록 바꾸고 Host
  회귀로 명령 순서를 고정했다. exact 로컬 UART/T13/docs/contract/style와 T13 두 역할 target,
  원격 Software gate를 통과했다. 일반 pair suite를 잘못 고른 첫 build는 image 검사에서 보드 기록 전
  거부해 보존했고 T13 전용 새 build만 사용했다. UART30 B는 예행1회와 본시험100/100을 통과했다.
  본시험은 cleanup303그룹·idle600·expected-fault200·최종 핀 반환을 독립 재감사했다.
  [90회차 실패·디버거·수정 gate·최종 PASS](evidence/t13-uart30-b-parity-start-order-2e4ec73/manifest.json).

현재 보드와17신호+GND 결선은 그대로이며 hardware process는 종료 상태다. QDEC와 handover는
사용자 지시대로 범위 밖이다. 다음은 실패 우선 원칙에 따라 I2S B와 CTS C01/C05를 먼저 다시
분리하고, 나머지 S33조건과 System OFF2조건을 완료한 뒤 S 감사를 거쳐 U 재결선을 안내한다.

## 2026-09-09 09:36 KST — I2S 원래 endpoint 경로 분리

이 진단은 formal S 조건을 PASS로 바꾸지 않으며 완료율은 **22/58(37.9%)**로 유지한다. 제품
I2S 구현이나 oracle을 완화하지 않았다. 동일 I2S20·DMA·48 kHz·32-bit stereo·클록 조건에서
두 data endpoint 배치만 바꾸어 원래 경로와 교환 경로를 비교했다.

- source6ba43ce0 원래 경로 `B P1.06 SDOUT -> A P1.07 SDIN`은 B 공급 중단 뒤7회차 재시작에서
  A 수신27 word가 불일치했다. A 패드 observer는3,821전이를 보았고 수신 데이터3,819와는2차이지만
  기대4,085보다264 부족했다. B TX RAM·DMA·I2S 설정과 양쪽 guard는 정상이었다. 따라서 A DMA/RAM
  내부보다 실제 A SDIN 패드 또는 그 앞의 B 출력·보드 간 경로가 먼저 소실된 경계다.
- sourcead62e1f1은 현재 두 data 선을 물리 변경하지 않고 반대로 사용하는 진단을 추가했다. 최초 실행은
  firmware dispatcher가 mode2를 거부해 I2S에 도달하지 못했고, sourceda070998이 이를 수정했다.
  다음 실행은 role1 sample729에서1 word 불일치로 중단됐다. 실패 레지스터의 `PSEL.SDOUT=P1.06`,
  `PSEL.SDIN=P1.07`은 공급 중단 전 단계가 여전히 원래 경로였음을 증명했다. 이 결과를 교환 경로
  실패로 쓰지 않았다.
- source4d01c262은 mode2일 때 공급 중단 전과 재시작 뒤 모두 같은 교환 경로를 선택하도록 Host runner를
  수정하고 명령 순서 단위시험을 추가했다. exact T13 Host134·style418·docs225·contract45, 두 역할
  target과 원격 Software7/7을 통과했다.
- 교환 경로 `B P1.07 SDOUT -> A P1.06 SDIN`은 B underrun 실제 관측100건과 STOP 뒤 새 seed 재시작
  **100/100**을 통과했다. A의100회 물리 전이 합계는 관측1,635,317, 기대1,635,145, 수신1,635,145이며
  회차별 관측-수신 최대 차이는4다. 실패·미확정0, cleanup203, 양쪽 idle400기록을 확인했다.

따라서 nRF54L15 I2S 주변장치·DMA·클록·공통 소프트웨어 경로는 교환 endpoint에서 정상이고, 남은
결함 경계는 원래 `B P1.06 출력/A P1.07 입력` endpoint 쌍과 그 사이 물리 경로다. 현재 고정 S 결선은
한 endpoint만 다른 핀에 연결할 수 없어 B P1.06과 A P1.07을 개별 분리하지 못한다. 이를 숨기고
교환 경로 결과를 formal 정상 soak로 소급하지 않는다. 실패·dispatcher 거부·선행 경로 오류·교환
100회와 exact gate 원본51개는 [I2S endpoint 진단 manifest](evidence/t13-i2s-endpoint-diagnosis-4d01c262/manifest.json)에
원문 SHA-256과 deterministic gzip으로 보존했다. 종료 시 두 보드 STOP, clock reference0, 핀 idle이며
hardware process는 없다. 다음은 아직 미완료인 CTS7(C01/C05 포함)을 원인 우선으로 재개한다.

## 2026-09-09 10:24 KST — C01 UART21 최초 framing과 TX 구동 대조

이 비교는 원인 진단이며 formal S PASS를 추가하지 않는다. 완료율은 **22/58(37.9%)**로
유지한다. 동일 source834e4d61·두 image·현재 S 결선·1 Mbit/s C01에서 첫 오류 DWT
watchpoint와 GPIO/UARTE/DMA/RAM을 함께 보존했다.

- 기본 `S0S1` 실행은 role1 UART21 lane1 오류 쓰기에서 먼저 멈췄다. error25와
  `driver_error=0x04`는 framing이고, RX DMA는4byte였다. 기대 첫4byte `C1 50 63 30`과
  실제 `50 AA B0 37`은 모두 달랐다. role2는 같은 시점에 오류0이고 TX DMA1024byte를
  완료했다. 양쪽 baud/config/PSEL은1 Mbit/s·8N1·A/B TX=P1.06·RX=P1.07이었고 CTS/RTS는
  분리 상태였다. 시작 barrier는 양쪽 수신 준비 뒤 송신을 해제했으며 DMA bus error와
  event queue overflow는 최초 원인이 아니었다.
- 펌웨어·이미지·배선을 바꾸지 않고 실패 방향인 `B P1.06 TX -> A P1.07 RX`의 B 출력
  DRIVE0/DRIVE1만 시작 직전에 `S0S1(0x003) -> H0H1(0x503)`로 바꿨다. CTS 유지/해제
  구간과 새 seed 재시작 구간 두 측정이 모두 끝났고 오류 watchpoint 적중은0이었다.
  종료 뒤 양쪽은 SLEEPING, lane 오류·active context·UARTE21 ENABLE/ERRORSRC가0이고
  P1.06/P1.07 `PIN_CNF=0`으로 복원됐다. 대량 지우기·recover·unlock은 없었다.
- 독립 감사는 두 실행의 역할·UID hash·HEX/ELF/build record hash가 같음을 확인하고,
  기본 구동의 최초 framing 1건과 고구동의 독립 시작2구간·적중0·종료 복원을 대조했다.
  [DWT 캡처·레지스터·RAM·대조 감사·gate 원본](evidence/t13-cts-drive-diagnosis-834e4d6/manifest.json)에
  34개 원본의 SHA-256과 deterministic gzip을 보존했다. 감사 초안이 실행별 flash 시간까지
  동일 비교해 중단된 첫 assertion도 숨기지 않았고, 이미지 identity 필드 비교로 교정했다.
- 제품 보완은 `dap_uart_disabled` 전기 프로필의 정확히1 Mbit/s UARTE TX만 nrfx 초기화
  뒤 H0H1으로 전환한다. `nrf_gpio_reconfigure`로 drive 필드만 바꾸므로 방향·입력·pull·sense와
  보드 확장 필드는 유지하고, 다른 속도·일반 connector·RX·SPI/TWI는 바꾸지 않는다.
  변경 작업 트리에서 전체 Host gate, contract45, docs225, style418, 두 역할 T13 target2/2
  warning0을 통과했다. 첫 style 실행의 clang-format 불일치는 보존하고 format 뒤 재검사했다.

다음은 이 변경을 clean source로 고정하고 동일 gate·원격 Host를 다시 확인한 뒤, 새 image의
C01 짧은 복구와 CTS 정식 대열을 수행한다. 현재 진단 성공을 새 source의 물리 PASS로 복사하지 않는다.
