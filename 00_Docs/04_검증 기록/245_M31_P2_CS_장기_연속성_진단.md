# 245 — M31 P2 CS 장기 연속성·중단 상태 진단

> 판정 갱신: 아래 `HOLD` 중 counter gap만을 이유로 한 판정은 당시 0-gap
> 기준이다. 현행 P2는 간헐 loss를 비차단 관찰값으로 보존한다. 유효 RTT 0인
> 불완전 raw·STOP 미확인 등 실제 실패는 면제하지 않는다. 최신 256-step
> 장시간 결과와 기준은 [260번](260_M31_P2_CS_누락_분류와_256_step_장시간.md),
> P2 세 축 완료는 [262번](262_M31_메모리_최적화_P2_세_축_완료.md)을 따른다.

## 판정 경계

두 exact NU54DK의 보안 연결과 Arduino `RasInitiator`/`RasReflector` 경로에서
CS raw 결과를 300~1,000개 수집했다. 모든 실행은 probe/COM 역할을 먼저
검증하고 자동 unlock 없이 sector flash 또는 reset-halt-drain-resume으로 시작했다.
원본에는 image SHA-256, 익명화한 probe hash, UART 및 STOP 결과가 있다.
이 진단 중 저장소에 미커밋 변경이 있었으므로 source는 clean 상태가 아니었으며,
각 결과는 해당 image·부하의 실기 판정이지 clean-source 릴리스 근거가 아니다.

| 실행 | 결과 | 원본 |
| --- | --- | --- |
| 기존 계측 image, 300 raw | **HOLD**. 완료·UART 300건은 같지만 counter 다섯 곳이 비연속. 양측 STOP. | [300건 원본](evidence/m31-p2-cs-53e8bbad/cs-300-raw-01.json) |
| controller 관찰 image, 300 raw | **국소 PASS**. 300건 연속·양측 STOP. 진단 코드로 timing/RAM이 달라졌으므로 결함 수정 근거가 아니다. | [관찰 300건](evidence/m31-p2-cs-53e8bbad/cs-300-raw-observer-01.json) |
| controller 관찰 image, 500 raw 1차 | **HOLD**. 누락 4건 중 세 건은 controller callback이 있었고 abort 표시는 없었다. 한 건은 subevent abort reason 2였다. | [관찰 500건 1차](evidence/m31-p2-cs-53e8bbad/cs-500-raw-observer-02.json) |
| 상태·step 수 확장 image, 500 raw 2차 | **HOLD**. 누락 13건 중 아홉 건은 `NO_CS_SYNC_RECEIVED`(reason 2), 네 건은 정상 완료 상태에서 92~96 step을 보고했다. 모두 callback 한 건씩이며 양측 STOP. | [관찰 500건 2차](evidence/m31-p2-cs-53e8bbad/cs-500-raw-observer-03.json) |
| abort 시 로컬 잠금 해제·늦은 RAS 보호 수정, 500 raw × 2 | **두 번 국소 PASS**. 각 실행의 counter 0~499 연속, `completed=500`, 양측 STOP. | [수정 1차](evidence/m31-p2-cs-53e8bbad/cs-500-raw-abortfix-04.json) · [수정 2차](evidence/m31-p2-cs-53e8bbad/cs-500-raw-abortfix-05.json) |
| 관찰 배열 없는 수정 image, 1,000 raw | **HOLD**. `completed=1,000`, UART 1,000건과 양측 STOP은 확인했으나 378→380 단발 누락이 남았다. | [수정 장기 실행](evidence/m31-p2-cs-53e8bbad/cs-1000-raw-clean-06.json) |
| 관찰 범위를 1,024 counter로 늘린 수정 image, 1,000 raw 시도 | **FAIL 보존**. 325·427·623 누락 뒤 624번에서 local/peer 6 step만 있고 valid RTT 0인 raw가 나와 판정기가 중단했다. 진단 출력이 길어 initiator STOP은 원 실행의 시간 안에 확인하지 못했다. | [확장 진단 실패](evidence/m31-p2-cs-53e8bbad/cs-1000-raw-diag-07.json) |
| 확장 진단 뒤 보드 복구 | **PASS**. 관찰 배열 없는 image로 다시 안전하게 기록하고 raw 1건·양측 STOP을 확인했다. 새 장기 연속성 PASS가 아니다. | [복구 원본](evidence/m31-p2-cs-53e8bbad/cs-cleanup-1-raw-08.json) |
| subevent abort 분리 2차 수정, 관찰 배열 없는 1,000 raw | **HOLD**. raw 1,000건·양측 STOP은 확인했으나 407→409, 605→607 두 곳에서 counter가 빠졌다. | [2차 수정 장기 실행](evidence/m31-p2-cs-53e8bbad/cs-1000-raw-v2-clean-09.json) |
| 2차 수정 + 1,024-counter 관찰, 1,000 raw | **HOLD**. 정상 callback 뒤 전달 누락 4건, RF sync abort reason 2 뒤 누락 4건. `completed=1,000`·양측 STOP. 계측 image의 RAM·timing은 다르다. | [2차 수정 진단](evidence/m31-p2-cs-53e8bbad/cs-1000-raw-v2-diag-10.json) |
| 단일 버퍼 경쟁 추적, 500 raw 시도 | **진단용 FAIL 보존**. 27번 callback에서 이전 26번 로컬 버퍼가 busy인 것을 확인했다. 진단 `printk`와 `Serial.print`가 같은 UART 줄에 섞여 판정기가 중단했으며, 양측 STOP·상대 disconnect는 확인했다. 누락률 판정에는 쓰지 않는다. | [버퍼 경쟁 원본](evidence/m31-p2-cs-53e8bbad/cs-500-raw-v2-trace-11.json) |
| 절차 간격 5→20 connection event, 관찰 배열 없는 300 raw | **HOLD**. 추가 RAM 없는 완화 실험이지만 143→146, 167→169 누락이 남았다. 양측 STOP. 이 설정을 제품 소스에 반영하지 않았다. | [간격 실험 원본](evidence/m31-p2-cs-53e8bbad/cs-300-raw-interval20-12.json) |
| 로컬 절차 버퍼 1→2개, 관찰 배열 없는 1,000 raw | **HOLD**. 1,000건 수집·양측 STOP이지만 12→14, 361→363 두 곳의 누락이 남았다. 정적 RAM은 같은 역할의 단일 버퍼 image 80,458→84,838 B로 4,380 B 증가했다. SDK·반사기 설정은 그대로였다. 이 후보 변경은 저장소 소스에서 되돌렸다. | [두 슬롯 실험 원본](evidence/m31-p2-cs-53e8bbad/cs-1000-raw-twoslot-13.json) |
| 두 슬롯 실험 뒤 복원 | **PASS**. 기존 단일 버퍼 image로 initiator를 sector flash해 raw 1건과 양측 STOP을 확인했다. 장기 연속성 PASS로 세지 않는다. | [복원 원본](evidence/m31-p2-cs-53e8bbad/cs-cleanup-1-raw-14.json) |

기존 라이브러리는 subevent가 abort되어도 procedure 전체가 `COMPLETE`면
`local_busy`를 유지했다. 해당 abort에는 상대 RAS callback이 오지 않을 수
있으므로 다음 정상 procedure가 `busy`로 연쇄 폐기될 수 있다. 또 늦게 도착한
이전 counter의 RAS callback이 현재 counter의 로컬 step과 잠금을 지웠다.
첫 수정은 abort 시 현재 로컬 step/잠금을 정리하고, counter가 다른 RAS callback은
현재 step을 건드리지 않게 했다. 그러나 확장 진단에서 325·427번은 **같은
procedure의 abort된 subevent 뒤 정상 subevent**가 들어온 것을 확인했다.
첫 수정의 counter 전체 폐기 정책이 두 번째 정상 callback까지 버렸다.
이에 procedure abort와 subevent abort를 분리하고 후자는 다음 subevent가
다시 로컬 step을 수집하도록 바꿨다. 두 번째 수정도 1,000건 두 image에서
연속성을 닫지 못했다. 컨트롤러의 **실제 CS 동기화 실패**를 가짜 raw 성공으로
바꾸지는 않는다.
위 500건 두 번의 PASS는 첫 수정 효과의 관찰이지
모든 RF 조건·fragmented RAS·최대 procedure의 완료 판정이 아니다.

623번은 controller callback 두 건 모두 abort가 없고 마지막 96 step을 보고했지만
Arduino raw로 전달되지 않았다. 624번의 6 step·RTT 0은 외부에서 유효한 거리
결과로 사용할 수 없다. 623번의 RAS 도착 순서/결합과 624번의 부분 결과 발생
조건은 미확정이다. 확장 진단의 `P2_CS_MISSING`은 STOP 시점에 controller가
앞서간 counter까지 출력해 수백 줄이 되었으므로 그 뒤쪽은 실제 누락으로
계산하지 않는다. 시험 sketch는 전달된 마지막 counter까지만 출력하도록 고쳤다.

진단 callback은 `NUCODE_P2_CS_DIAGNOSTICS`를 켠 시험 sketch에만 넣었다.
controller callback의 `step_data_buf`는 callback 순서에 따라 다른 소비자가
길이를 변경할 수 있으므로 그 길이 flag만으로 controller 원본 buffer가
비었다고 단정하지 않는다. 위의 abort status/reason·reported step은 별도
header 값이다. 512-counter 진단 image의 정적 RAM 95,310 B와
1,024-counter 확장 진단의 110,158 B는 관찰 배열을 포함하며,
관찰 배열이 없는 수정 image는 80,458 B였다. 이 차이를 제품 최적화 절감량이나
동일 조건의 stack high-water로 비교하지 않는다.

첫 관찰 배열 없는 1,000건 실행의 CS initiator stack 최고치는 `BT RX WQ`
1,520/3,200 B, `BT LW WQ` 936/2,104 B, `BT GATT DM WQ` 352/1,304 B,
`MPSL Work` 504/1,024 B, main 1,048/8,192 B, ISR0 600/2,048 B다.
이는 해당 정상·단발 누락 부하의 관찰값일 뿐, 내부 SDC pool high-water나
모든 연결/오류 경로의 안전 여유가 아니다. 따라서 stack·heap·controller
pool을 이번 결과만으로 축소하지 않는다.

## 당시 진단 과제와 후속 경과

아래 원인 추적 목록은 당시 0-gap 정책에서 작성했다. 현행 필수 gate가 아니며
이 기록 이후의 P2 세 축 완료 판정은 [262번](262_M31_메모리_최적화_P2_세_축_완료.md)을 따른다.

2차 수정 진단에서 정상 callback 뒤 누락된 89·149·262·408번을 RAS 도착 순서·
로컬 단일 버퍼의 busy/drop·queue 상태로 분류해야 한다. 90·106·446·971번은
RF sync abort reason 2이며 유효 raw로 세지 않는다. 623번 정상 callback 뒤
누락과 624번 불완전 raw도 별도 원인을 추적한다.
진단 전용 image의 27번 `busy`는 하나의 로컬 절차 버퍼가 상대 RAS
도착 전 다음 controller callback을 거부하는 경로를 직접 보여준다.
다만 한 건의 추적만으로 모든 정상 callback 누락의 원인을 동일하게 확정하지 않는다.
고정 NCS의 `nrf/subsys/bluetooth/services/ras/rreq/ras_rreq.c`도 실시간
구독마다 `counter_in_progress`와 peer 출력 버퍼를 각각 하나씩 사용한다.
완료 callback 뒤 그 버퍼를 재설정하므로, 로컬 버퍼만 두 개로 늘리는 변경을
RAS 전달 연속성의 충분조건으로 간주하지 않는다. 로컬 busy와 RAS segment
도착·완료 순서를 같은 counter 기준으로 함께 계측해야 한다. 이는 소스 구조에서
도출한 다음 진단 조건이지, 현재 누락의 단일 원인 확정은 아니다.
실제 두 로컬 슬롯 시험도 누락을 제거하지 못했으므로 추가 4,380 B를 제품 RAM에
예약하지 않았다. 해당 시험은 단일 슬롯 경쟁 외의 RF abort·RAS 전송 또는
결합 경로가 남을 수 있음을 보여줄 뿐, 각 누락의 원인을 특정하지 않는다.
후속 [250번 native 비교](250_M31_P2_native_CS_비교와_DF_재진단.md)에서는
고정 Nordic RAS 계측 복사본도 1,000개 유효 RAS까지 counter gap 4건과
7건을 보였다. abort와 단일 버퍼 busy/mismatch를 따로 계수했다. 따라서
절대적인 0-gap 문턱은 고정 native 경로에도 성립하지 않는다. 단, 이 결과로
Arduino의 정상 callback 뒤 누락이나 불완전 raw를 무시하지 않는다.
절차 간격을 네 배로 늘리는 완화안만으로도 누락이 사라지지 않아, 원래
5-event 설정을 유지한다. 간격만 바꾼 image를 native 동등 비교나 수정 완료
근거로 사용하지 않는다.
최대 procedure·fragmented RAS·재연결/오류 복구의 메모리 및 연속성,
반사기 역할의 동적 사용량과 native 동등 조건 비교도 남는다. P2 전체와
M31-W05/W06은 이 기록만으로 완료하지 않는다.

후속 [255번 진단](255_M31_P2_CS_256_step_분할_RAS_진단.md)은
실제 256-step·분할 RAS 정상 수신 10/10을 확인했다. 같은 image의
100건에는 counter gap 1이 남아 이 기록의 누락 원인 조사를 닫지 못했다.

후속 [252번 장절차·반복 계수 진단](252_M31_P2_CS_장절차_반복계수_경계_진단.md)은
진단용 길이·반복 설정 두 가지에서 각각 raw 10건·양측 STOP을 통과했지만,
실제 수신 step은 93~98에 머물렀다. 256-step 상한과 RAS 분할은 검증되지
않았고, 이 기록의 정상 callback 뒤 누락도 해결되지 않았다.
