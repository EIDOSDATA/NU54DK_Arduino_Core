# NU54DK Arduino Core — NCS v3.4.0 기능·예제 지원 매트릭스

| 항목 | 내용 |
| --- | --- |
| 문서 ID | NCS-COVERAGE-001 |
| 문서 개정 | 3.3 |
| 문서 상태 | `v0.2.0` 공개 범위 + `v0.3.0` 개발 검증 상태 |
| 적용 제품 버전 | `v0.2.0` 정식 / `v0.3.0` 개발 |
| 최종 갱신일 | 2026-09-01 |
| 작성자 | Quantum / NUCODE |
| 기준 | NCS v3.4.0 / Zephyr 4.4.0 / NU54DK CPUAPP |

이 프로젝트의 목표는 NCS sample을 모두 같은 `.ino` wrapper로 다시 작성하는 것이 아니다.
기능마다 Arduino wrapper, NCS direct example, validated profile, application template 또는
명시적 제외 중 적합한 경로를 제공한다. **Build 성공, 제품 지원과 HIL 성공을 서로 같은
상태로 취급하지 않는다.**

## 1. 세 가지 독립 분류

### 제공 경로

| 경로 | 의미 |
| --- | --- |
| Arduino wrapper | 반복 사용 가치가 높은 안정적인 사용 사례를 Arduino API로 제공 |
| NCS direct | `.ino`/`.cpp`에서 Zephyr/NCS 공개 API를 직접 사용 |
| Validated profile | Kconfig, DTS, partition 또는 sysbuild 조합이 기능의 핵심 |
| Application template | Matter/DFU처럼 독립 application 구조를 유지 |
| External library | NUCODE가 소유하지 않는 Arduino library와의 compile 호환성 |
| Excluded | hardware·제품 정책 또는 검증 비용 때문에 제공하지 않음 |

### 제품 상태

| 상태 | 의미 |
| --- | --- |
| `supported` | 공개 API/예제/문서와 필요한 자동·HIL 근거가 있음 |
| `partial` | 명시한 pin, mode 또는 외장 hardware 범위만 지원 |
| `build-only` | compile/link만 검증했으며 runtime 지원이 아님 |
| `planned` | 승인된 다음 버전 계획이며 아직 공개 지원이 아님 |
| `deferred` | 가치가 있지만 현재 제품선 밖 |
| `not-applicable` | MCU, NU54DK 회로 또는 제품 구조와 맞지 않음 |

### 검증 상태

| 상태 | 의미 |
| --- | --- |
| `inventory` | upstream 경로와 적용 가능성만 조사 |
| `build-pass` | 고정 환경에서 compile/link 성공 |
| `semantic-pass` | API 의미와 오류 경로를 자동 시험 |
| `hil-pass` | 실제 NU54DK와 필요한 fixture에서 검증 |
| `not-run` | 실행하지 않았으며 PASS로 해석하지 않음 |
| `build-fail` | 적용 build가 실패했고 원인을 기록 |

하나의 기능은 세 축을 모두 가진다. 예를 들어 외부 sensor library는 `External library / build-only /
build-pass`일 수 있으며, 이를 `supported / hil-pass`로 확대하면 안 된다.

## 2. v0.2.0 공개 기능

| 기능 | 제공 경로 | 제품 상태 | 검증·경계 |
| --- | --- | --- | --- |
| GPIO·interrupt | Arduino wrapper + NCS direct | `supported` | 정식 package의 LED/button HIL |
| 시간·delay·scheduler | Arduino wrapper | `supported` | semantic + HIL |
| DAP UART Serial | Arduino wrapper | `partial` | 115200 8N1, native USB CDC 아님 |
| I2C Wire | Arduino wrapper + direct | `partial` | 지정 bus와 BQ25186 read HIL |
| SPI | Arduino wrapper + direct | `partial` | SPI00 physical loopback HIL |
| ADC | Arduino wrapper + direct | `partial` | A0 raw-read HIL; 입력 전압 정확도 `not-run` |
| PWM | Arduino wrapper + direct | `partial` | P1.10 driver 호출 HIL; 외부 파형 `not-run` |
| GRTC counter·alarm | Board/System wrapper + direct | `supported` | callback와 timed System OFF HIL |
| Watchdog | Board/System wrapper | `supported` | stop·expiry reset HIL |
| System OFF | Board/System wrapper + profile | `supported` | GRTC와 사용자 SW0 wake HIL |
| Settings/ZMS | Board/System wrapper + direct | `supported` | reset persistence HIL |
| BQ25186 | 제한된 Board/System wrapper | `partial` | ID read HIL; write 전기 HIL `not-run`, NTC 미지원 |
| BLE NUS | Arduino wrapper + NCS direct + `ble` profile | `supported` | Peripheral/Central 두 보드 HIL |
| Sensor direct | NCS direct | `build-only` | NU54DK build-pass, sensor runtime/HIL `not-run` |
| Adafruit LSM6DS3TR-C | External library | `build-only` | Arduino compile-pass, package 미포함 |
| Crypto RNG | NCS direct | `build-only` | 공식 board와 NU54DK build-pass, wrapper 없음 |

API별 세부 의미는 [Arduino API 지원 범위](04_Arduino_API_지원_범위.md), Board/System과 BLE
계약은 각각 [Board/System 설계](<../03_펌웨어 설계/05_NU54DK_Board_System_API.md>)와
[BLE NUS 설계](<../03_펌웨어 설계/06_BLE_NUS_API.md>)를 따른다.

정식 `v0.2.0` archive는 public library 4개와 예제 14개를 배포했다. 아래 개발 수치를 이 역사적
package 범위에 소급 적용하지 않는다.

## 3. v0.3.0 개발 검증 상태

현재 source tree는 public library 6개와 예제 27개를 가진다. 기존 19개는 Arduino CLI에서 19/19
compile을 통과했고 AC-02B 8개도 고정 source snapshot에서 8/8 compile을 통과했다. 설치 예제
discovery는 27/27이다. 전체 27개 clean package compile은 M22에서 다시 고정한다. 개별 작업선의
상태는 다음과 같고, 전체 `v0.3.0` 정식 지원은 M22 전에는 선언하지 않는다.

| 작업 | 현재 상태 | 검증·남은 경계 |
| --- | --- | --- |
| AC-01 Core/GPIO/시간 | 자동 검증 완료 | exact commit `ac10ba3b253bd6bf76bcf73aa2c79278304908a4` target/HIL PASS |
| AC-02 Peripheral/Analog | 진행 중 — AC-02B 자동 구현·build PASS | owner/resource manager, runtime handover와 Serial1/Wire/SPI/ADC/PWM/tone/Servo 공개 API의 host/target build PASS; exact 8선 물리 HIL 대기 |
| AC-03 Storage/생태계 | 미착수 | facade·제3자 library matrix 미실행 |
| M19 BLE Core/GAP | 자동 검증 완료 | exact commit `0103a8434ac205a953c981385ae26a2a64aeeccc` 두 보드 RF HIL PASS |
| M20 범용 GATT | 자동 검증 완료 | exact commit `0103a8434ac205a953c981385ae26a2a64aeeccc` 두 보드 RF HIL PASS |
| M21 BLE security·BAS/DIS/HID | 완료 | Core `065d4f5` exact 두 보드 RF HIL + `d1902b1` Windows 11 HID·bond 복원 + host 39/39 PASS |
| M22 통합 릴리스 | 미착수 | AC-02·AC-03 완료 뒤 package·clean Windows·RC/stable gate |

검증 상세는 [AC-01](<../04_검증 기록/22_AC-01_GPIO_호환성_검증.md>),
[M19](<../04_검증 기록/23_M19_BLE_Core_GAP_검증.md>),
[M20](<../04_검증 기록/24_M20_범용_GATT_검증.md>),
[M21](<../04_검증 기록/25_M21_BLE_보안과_표준_Profile_검증.md>)과
[AC-02A](<../04_검증 기록/26_AC-02A_핀과_주변장치_소유권_기준선.md>) 기록을 따른다.

## 4. 조사했지만 v0.2.0에서 지원하지 않는 기능

| 기능 | 제공 경로 후보 | 제품 상태 | 현재 증거·차단점 |
| --- | --- | --- | --- |
| BLE Core/GAP | wrapper + `ble` profile | `planned` | v0.2 미지원; v0.3 M19 자동 검증 완료 |
| 범용 GATT | wrapper + direct | `planned` | v0.2 미지원; v0.3 M20 자동 검증 완료 |
| BLE security·BAS/DIS/HID | 별도 library + profile | `planned` | v0.2 미지원; v0.3 개발 tree M21 자동 RF HIL·Windows 11 실제 HID 검증 완료, M22 stable gate 대기 |
| BLE Mesh·Channel Sounding·ISO | profile + direct | `deferred` | 별도 memory/radio/HIL gate 필요 |
| PSA Crypto/Cracen 확대 | 얇은 wrapper + direct | `deferred` | M23 제품선 후보 |
| MCUboot/DFU | profile/template | `deferred` | partition, update와 recovery 정책 필요 |
| TF-M | `/ns` target + profile | `deferred` | secure boundary·recovery HIL 필요 |
| IEEE 802.15.4 | direct + radio profile | `deferred` | 공식 build-pass, NU54DK NVMC symbol build-fail |
| OpenThread | facade + direct + profile | `deferred` | NU54DK build-pass만 확보, runtime/network HIL 없음 |
| Matter | application template | `deferred` | 공식 build-pass, NU54DK factory-data partition 누락 |
| ESB/MPSL | wrapper + radio profile | `deferred` | radio resource·coexistence gate 필요 |
| I2S/PDM·QDEC·Comparator | direct + 외장 hardware | `deferred` | fixture와 사용자 가치 미확정 |
| Wi-Fi/network | nRF70 add-on profile | `deferred` | 외장 nRF70 필요 |
| NFC | Excluded | `not-applicable` | NU54DK I2C22 P1.2/P1.3 자원과 충돌 |
| Native USB device | Excluded | `not-applicable` | nRF54L15 target에 native USB peripheral 없음 |
| Cellular/DECT·nRF Audio | Excluded | `not-applicable` | 대상 SoC/제품 구조와 불일치 |

`planned`와 `deferred`는 지원 상태가 아니다. 자세한 다음 순서는
[Master roadmap](02_구현_로드맵.md)과 [v0.3.0 마일스톤](07_v0.3.0_구현_마일스톤.md)에서
관리하며 이 매트릭스에 장기 계획을 반복하지 않는다.

## 5. Machine-readable coverage 단일 원본

M17 이후 기능별 exact revision, license, hardware, route, status와 evidence는
`coverage/ncs-v3.4.0/`이 소유한다.

- [Coverage manifest](../../coverage/ncs-v3.4.0/manifest.json)
- [자동 생성 Markdown 요약](../../coverage/ncs-v3.4.0/generated/summary.md)
- [자동 생성 JSON 요약](../../coverage/ncs-v3.4.0/generated/summary.json)
- [M17 검증 기록](<../04_검증 기록/19_M17_NCS_기능과_예제_Coverage_기준선.md>)
- [v0.3.0 검증 기록 디렉터리](<../04_검증 기록>)

`tools/coverage/m17_coverage.py`는 schema, exact repository/path/revision, license, profile,
hardware, evidence와 record SHA-256을 fail-closed로 확인한다. `generated/`는 직접 편집하지
않는다. 이 문서의 표는 사용자용 분류이며 9개 M17 record의 exact 값을 복제하지 않는다.

## 6. NU54DK 자원 제약

- BLE, IEEE 802.15.4와 ESB는 같은 radio를 공유한다. 검증된 MPSL/multiprotocol profile 없이
  동시에 시작하지 않는다.
- P1.2/P1.3은 I2C22로 사용하며 NFC와 동시에 사용할 수 없다.
- NU54DK에는 Nordic DK와 같은 MX25R64 외장 flash가 없다. 외장 flash 기반 Settings, Mesh나
  DFU 구성을 동일하게 적용하지 않는다.
- 동일 instance를 공유하는 SPI/UART 조합은 profile에서 상호 배타로 관리한다.
- native USB device가 없으므로 `SerialUSB`, Keyboard와 Mouse를 USB API로 제공하지 않는다.

## 7. 지원 판정 규칙

1. Upstream sample 수를 wrapper 수나 지원률로 바꾸지 않는다.
2. 공식 DK build를 NU54DK runtime PASS로 표시하지 않는다.
3. Hardware가 없으면 HIL PASS로 표시하지 않는다.
4. 실제 값을 얻을 수 없는 radio/counter 정보를 합성하지 않는다.
5. Profile이나 application template이 맞는 기능을 억지로 Arduino singleton으로 만들지 않는다.
6. Zephyr driver가 있는 장치를 일반적인 raw-register Arduino API로 우회하지 않는다.
7. 지원 상태 변경은 coverage record, 예제, 자동 시험과 필요한 HIL을 함께 갱신한다.

BQ25186의 제한 adapter는 board 전용 예외다. DTS node disabled, read-only `pmicBegin()`, 매 boot
RAM write 승인, 허용 field, 범위 검사와 reserved-bit 보존을 적용한다. 이 예외를 다른 driver의
임의 raw register 접근 허용으로 확대하지 않는다.
