# NU54DK Arduino Core — 제품 로드맵과 구현 마일스톤

| 항목 | 내용 |
| --- | --- |
| 문서 ID | ROADMAP-MASTER-001 |
| 문서 개정 | 5.7 |
| 문서 상태 | `v0.2.0` 정식 공개 / **RC3 memory-contract 교정·공개 재검증 준비** |
| 현재 정식 버전 | `v0.2.0` |
| 다음 목표 버전 | `v0.3.0` |
| 최종 갱신일 | 2026-09-02 |
| 작성자 | Quantum / NUCODE |
| 기준 SDK | nRF Connect SDK v3.4.0 |
| 기준 Zephyr | Zephyr 4.4.0 |
| 기준 보드 target | `nrf54l15dk/nrf54l15/cpuapp/nu54dk` |
| 실행 구조 | Loader 없는 Native Full Zephyr 정적 build |

---

## 1. 문서 목적과 소유권

이 문서는 제품 단계의 **순서, 현재 상태, 제품선과 상세 문서 링크**를 관리한다. 마일스톤별
명령, commit, 시험 수치와 HIL log를 복제하지 않는다.

문서별 소유권은 다음과 같다.

| 정보 | 단일 원본 |
| --- | --- |
| 전체 마일스톤 상태와 제품 순서 | 이 문서 |
| 아키텍처 선택 근거 | [ADR-0001](<../00_사전 리서치/01_개발_방식_비교_및_아키텍처_결정.md>) |
| 구성 profile과 예제 정책 | [ADR-0002](<../00_사전 리서치/02_Arduino_구성_프로필과_예제_노출_결정.md>) |
| 현재 Arduino API 지원 | [Arduino API 지원 범위](04_Arduino_API_지원_범위.md) |
| NCS 기능별 제공 경로와 상태 | [NCS 기능·예제 지원 매트릭스](06_NCS_3.4.0_기능과_예제_지원_매트릭스.md) |
| 마일스톤 명령·revision·시험 결과 | `00_Docs/04_검증 기록/`의 단계별 기준선 |
| 공개 package identity와 known issues | `00_Docs/05_릴리스/<version>/` |

완료된 단계의 상세 결과가 바뀌지 않는 한 이 문서에는 요약과 링크만 유지한다.

---

## 2. 제품 원칙

1. Arduino Core는 Zephyr를 대체하지 않고 Zephyr application 안의 호환 계층으로 구현한다.
2. 보드 GPIO, pinctrl, peripheral과 runner는 고정 board submodule이 소유한다.
3. 첫 제품선은 NCS v3.4.0과 Zephyr 4.4.0에 고정한다.
4. 일반 사용자는 `.ino`, library와 검증된 Tools profile을 사용한다.
5. 전문가는 명시적 `prj.conf`·overlay와 Zephyr/NCS 공개 API를 사용할 수 있다.
6. Arduino 사용자 예제는 `libraries/*/examples`를 단일 원본으로 사용한다.
7. 정상 build에서 무조건 `pristine always`를 사용하지 않는다.
8. CMSIS-DAP V2/pyOCD를 기본 upload 경로, 외장 J-Link를 선택 경로로 둔다.
9. Build PASS를 runtime, HIL 또는 제품 지원 PASS로 확대하지 않는다.
10. RC와 stable artifact는 exact source와 package metadata에 고정하고 같은 version을 덮어쓰지 않는다.
11. Loaderless 기본 layout은 영구 저장소를 제외한 RRAM 전체를 단일 application에 제공한다.
    MCUboot/DFU dual-slot은 실제 update·rollback 기능과 함께 검증한 고급 선택으로만 제공한다.

---

## 3. 단계 상태

| 상태 | 의미 |
| --- | --- |
| 완료 | 완료 기준과 필수 검증을 통과했고 증거를 보관함 |
| 진행 중 | 구현 또는 검증을 시작했으나 완료 기준을 모두 통과하지 않음 |
| 계획 | 제품 범위와 순서를 정했지만 아직 구현을 시작하지 않음 |
| 대기 | 선행 단계 또는 결정 gate를 기다림 |
| 차단 | 기술·법적·hardware 문제 때문에 진행할 수 없음 |
| 재검토 | 전제가 바뀌어 범위나 구조를 다시 결정해야 함 |

단계 번호는 구현 순서이며 일정 날짜가 아니다. `계획`과 `대기`는 지원 상태가 아니며, code나
header가 존재해도 완료 증거 전에는 API matrix를 상향하지 않는다.

`AC-01`~`AC-03`은 전역 마일스톤 번호가 아니라 `v0.3.0`의 Arduino Compatibility 하위 작업
package다. 기존 M19~M34 번호를 바꾸지 않으며, AC 트랙은 BLE 트랙과 병렬로 진행한 뒤 M22
릴리스 gate에서 합류한다.

---

## 4. 전체 단계 요약

| 단계 | 제품선 | 상태 | 핵심 결과 또는 목표 |
| --- | --- | --- | --- |
| M0 | `v0.1.0` | **완료** | 저장소와 고정 board submodule 기준선 |
| M1 | `v0.1.0` | **완료** | NCS 도구 환경과 NU54DK build·flash 기준선 |
| M2 | `v0.1.0` | **완료** | Zephyr module, C++ runtime과 Core 골격 |
| M3 | `v0.1.0` | **완료** | `setup()/loop()`, GPIO·시간과 scheduler 수직 경로 |
| M4 | `v0.1.0` | **완료** | ArduinoCore-API revision·license·통합 계약 |
| M5 | `v0.1.0` | **완료** | Arduino CLI에서 Full Zephyr image까지 Build Adapter |
| M6 | `v0.1.0` | **완료** | 기본 API, Serial과 GPIO interrupt |
| M7 | `v0.1.0` | **완료** | Wire, SPI, ADC와 PWM의 제한된 NU54DK 경로 |
| M8 | `v0.1.0` | **완료** | pyOCD 기본 upload/debug와 J-Link 선택 경로 |
| M9 | `v0.1.0` | **완료** | Persistent build, cache와 재현성 |
| M10 | `v0.1.0` | **완료** | Boards Manager와 clean Windows 수명주기 |
| M11 | `v0.1.0` | **완료** | `v0.1.0` RC 검증과 stable 공개 |
| M12 | `v0.2.0` | **완료** | CI/CD와 고정 환경 재현 build |
| M13 | `v0.2.0` | **완료** | 구성 profile, feature resolver와 Arduino 예제 UX |
| M14 | `v0.2.0` | **완료** | Core API·DTS Variant 정리와 신규 pin 검증 |
| M15 | `v0.2.0` | **완료** | NU54DK board/system library와 승인된 HIL 범위 |
| M16 | `v0.2.0` | **완료** | NUS Peripheral/Central Stream과 BLE profile |
| M17 | `v0.2.0` | **완료** | NCS v3.4.0 machine-readable coverage 기준선 |
| M18 | `v0.2.0` | **완료** | RC 공개 검증과 `v0.2.0` stable 공개 |
| M19 | `v0.3.0` | **자동 검증 완료** | BLE Core/GAP, advertising, scanning과 connection lifecycle |
| M20 | `v0.3.0` | **자동 검증 완료** | 범용 GATT server/client와 characteristic operation |
| M21 | `v0.3.0` | **완료** | exact 두 보드 RF HIL과 Windows 11 pairing·HID 입력·bond 복원 PASS |
| AC-01 | `v0.3.0` | **자동 검증 완료** | Core·GPIO·시간 호환성 기준선 `(난이도: 상)` |
| AC-02 | `v0.3.0` | **완료** | exact `0b7f892` 3-wire HIL에서 runtime Serial1/Wire/SPI와 A0 ADC→PWM handover PASS `(난이도: 상)` |
| AC-03 | `v0.3.0` | **완료** | exact `0b7f892` 두 보드에서 1024 B EEPROM·32 KiB LittleFS 영속성·복구·정리 PASS `(난이도: 상)` |
| M22 | `v0.3.0` | **RC3 memory-contract 교정·공개 재검증 준비** | Loaderless 1,456 KiB application 경계와 8개 library·29개 예제, 실제 Upload, public clean-room lifecycle 재검증 |
| M23 | `v0.4.0` | **계획** | Storage·PSA Crypto/Cracen 공개 경계 |
| M24 | `v0.4.0` | **계획** | 검증된 Memory layout 선택, MCUboot·DFU profile과 update/rollback 구조 |
| M25 | `v0.4.0` | **계획** | TF-M, secure boundary와 recovery 검증 |
| M26 | `v0.4.0` | **계획** | `v0.4.0` security/update 제품선 릴리스 |
| M27 | `v0.5.0` | **계획** | Radio profile 기반과 NU54DK 적용 build 교정 |
| M28 | `v0.5.0` | **계획** | IEEE 802.15.4와 ESB, physical radio HIL |
| M29 | `v0.5.0` | **계획** | OpenThread와 network 계층 |
| M30 | `v0.5.0` | **계획** | `v0.5.0` radio/network 제품선 릴리스 |
| M31 | `v0.6.0` | **계획** | Matter partition, factory data와 provisioning 기반 |
| M32 | `v0.6.0` | **계획** | Matter application template와 Arduino UX 경계 |
| M33 | `v0.6.0` | **계획** | Matter commissioning·fabric·runtime HIL |
| M34 | `v0.6.0` | **계획** | `v0.6.0` Matter 제품선 릴리스 |

---

## 5. 완료된 `v0.1.0` 기준선

M0~M11은 `v0.1.0` stable로 완료됐다. 각 단계의 실제 명령, 환경, 결과와 범위 제외는 다음
증거를 따른다.

| 단계 | 증거 |
| --- | --- |
| M0 | [저장소 구조와 소유권](01_저장소_폴더_구조.md) |
| M1 | [도구 환경과 보드 기준선](<../04_검증 기록/01_M1_도구와_보드_기준선.md>) |
| M2 | [Zephyr module과 runtime 기준선](<../04_검증 기록/02_M2_Zephyr_Module과_Runtime_기준선.md>) |
| M3 | [GPIO·시간·Scheduler 기준선](<../04_검증 기록/03_M3_GPIO_시간과_Scheduler_기준선.md>) |
| M4 | [ArduinoCore-API 계약 기준선](<../04_검증 기록/04_M4_ArduinoCore_API_계약_기준선.md>) |
| M5 | [Arduino CLI Build Adapter 기준선](<../04_검증 기록/05_M5_Arduino_CLI_Build_Adapter_기준선.md>) |
| M6 | [기본 API·Serial·interrupt 기준선](<../04_검증 기록/06_M6_기본_Arduino_API_Serial과_인터럽트_기준선.md>) |
| M7 | [Wire·SPI·ADC·PWM 기준선](<../04_검증 기록/07_M7_Wire_SPI_ADC_PWM_기준선.md>) |
| M8 | [Upload와 debug 기준선](<../04_검증 기록/08_M8_업로드와_디버그_기준선.md>) |
| M9 | [증분 build·cache·재현성 기준선](<../04_검증 기록/09_M9_증분_빌드_캐시와_재현성_기준선.md>) |
| M10 | [Boards Manager·clean Windows 기준선](<../04_검증 기록/10_M10_Boards_Manager_패키징과_Clean_Windows_기준선.md>) |
| M11 | [RC1 역사 기록](<../04_검증 기록/11_M11_v0.1.0_rc1_릴리스_후보_기준선.md>) · [RC2 공개 후 검증](<../04_검증 기록/12_M11_v0.1.0_rc2_공개_후_수동_검증.md>) · [stable 공개 기록](<../04_검증 기록/13_v0.1.0_정식_릴리스_공개_기록.md>) |

`v0.1.0`의 지원 범위와 제약은 해당 tag의 릴리스 문서를 역사적 단일 원본으로 사용한다.

---

## 6. 완료된 `v0.2.0` 기준선

M12~M18은 `v0.2.0` stable로 완료됐다. 상세 작업·완료 기준은
[v0.2.0 구현 마일스톤](05_v0.2.0_구현_마일스톤.md), 실행 증거는 다음 기록을 따른다.

| 단계 | 증거 |
| --- | --- |
| M12 | [CI/CD와 재현 build 기준선](<../04_검증 기록/14_M12_CI_CD_기준선.md>) |
| M13 | [구성 profile과 예제 배포 검증](<../04_검증 기록/15_M13_구성_프로필_검증.md>) |
| M14 | [Core API와 Variant 기준선](<../04_검증 기록/16_M14_Core_API와_Variant_기준선.md>) |
| M15 | [NU54DK Board/System 기준선](<../04_검증 기록/17_M15_NU54DK_Board_System_기준선.md>) |
| M16 | [BLE NUS 기준선](<../04_검증 기록/18_M16_BLE_NUS_기준선.md>) |
| M17 | [NCS 기능·예제 Coverage 기준선](<../04_검증 기록/19_M17_NCS_기능과_예제_Coverage_기준선.md>) |
| M18 | [RC1·RC2 공개 검증](<../04_검증 기록/20_M18_v0.2.0_rc1_공개_검증과_rc2_교정.md>) · [stable 공개 기록](<../04_검증 기록/21_v0.2.0_정식_릴리스_공개_기록.md>) |

Stable source, package checksum, index와 공개 asset identity는
[v0.2.0 릴리스 문서](../05_릴리스/v0.2.0/README.md)가 소유한다. RC1·RC2와 stable의 검증
범위는 서로 확대 해석하지 않는다.

`v0.2.0`에서 완료된 BLE 범위는 NUS Peripheral/Central Stream이다. 개발 tree의 범용 GAP/GATT,
bonding, SMP와 표준 profile은 M19~M21 구현·검증을 완료했지만 M22 stable package 전에는 정식
`v0.3.0` 지원으로 표시하지 않는다.

M17의 sensor·crypto·radio build 결과는 route와 적용 가능성 분류다. Build-only 또는 공식 DK
PASS를 NU54DK runtime이나 Arduino wrapper 지원으로 확대하지 않는다.

---

## 7. `v0.3.0` 진행 상태 — M19~M22와 AC-01~AC-03

상세 API, 완료 조건과 제외 범위는
[v0.3.0 구현 마일스톤](07_v0.3.0_구현_마일스톤.md)을 단일 원본으로 사용한다.

```text
BLE:           M19 BLE Core/GAP
                 → M20 범용 GATT server/client
                 → M21 pairing·bonding·표준 profile ─┐
                                                     ├→ M22 v0.3.0 RC/stable
Compatibility: AC-01 Core·GPIO·시간
                 → AC-02A 핀·주변장치 소유권 기반
                 → AC-02B runtime handover·tone·Servo
                 → AC-03 Storage·library 호환성 ─────┘
```

BLE 트랙과 Compatibility 트랙은 M18 완료 기준선에서 병렬로 시작할 수 있다. 각 트랙 내부의
선행 순서는 지키며, M22는 M21과 AC-03이 모두 완료되고 양쪽 회귀 시험이 연결된 뒤에만 시작한다.

공개 검증된 `v0.3.0-rc.2`는 public library 8개와 예제 29개를 제공한다. `Standard peripherals`
22개와 BLE 7개를 M22 exact 목록으로 compile했다. AC-03은 EEPROMPersistence와
LittleFSPersistence를 추가했다. 이 개발 수치를 공개 stable `v0.2.0`의 library 4개·예제 14개
기록에 소급하지 않는다.

### M19 — BLE Core와 GAP

- 상태: **자동 검증 완료** — exact commit `0103a8434ac205a953c981385ae26a2a64aeeccc`의 두 보드 RF HIL PASS
- 범위: lifecycle, local name, advertising data·interval, scan filter와 connection event
- 확인한 gate: host/target contract, 두 보드 advertise·scan·connect·reconnect와 main-thread callback
- 이력: 첫 `ac10ba3` HIL에서 자동 PHY 요청이 `link-request` 실패를 만들었고, 지원 가능한 PHY만
  명시적으로 요청하도록 고친 `0103a843`에서 PASS했다.

### M20 — 범용 GATT

- 상태: **자동 검증 완료** — exact commit `0103a8434ac205a953c981385ae26a2a64aeeccc`의 두 보드 RF HIL PASS
- 범위: 16/128-bit UUID, service/characteristic, read/write/notify/indicate와 generic client
- 확인한 gate: value·ATT MTU·CCC·reconnect 경계와 두 보드 server/client HIL

### M21 — BLE security와 표준 profile

- 상태: **완료**
- 자동 증거: Core `065d4f5` exact 두 보드 RF HIL PASS
- 수동 증거: Core `d1902b1` Windows 11 pairing·실제 HID 입력·재부팅 bond 복원 PASS
- 회귀: M21 host 39/39, 전체 host 408개 중 406 PASS·2 skipped, CI contract 33/33
- 범위: pairing, bonding, SMP, passkey, bond 관리, BAS, DIS와 BLE HID
- 핵심 gate: persistence·삭제·실패 negative, BAS/DIS/HID 자동 검증과 Windows OS 수동 호환성

### AC-01 — Core·GPIO·시간 Compatibility 기준선

- 상태: **자동 검증 완료** — exact commit `ac10ba3b253bd6bf76bcf73aa2c79278304908a4`의
  P2.5↔P2.6 loopback와 SW0 self-drive HIL PASS
- 난이도: **상** — GPIO interrupt 재무장, 정밀 timing과 Zephyr scheduler/IRQ 의미를 함께 고정해야 함
- 핵심 범위: 안전한 connector GPIO 확대, `OUTPUT_OPENDRAIN`, level interrupt, `pulseIn*`,
  `shiftIn`/`shiftOut`와 전역 IRQ 제어 API의 Zephyr-safe 계약
- 핵심 gate: pin/resource 충돌 negative, timeout·preemption·interrupt storm 경계와
  `P2.5(P2-18)↔P2.6(P2-19)` 단일 보드·내부 pull-up 고정 loopback HIL; 외부 pull-up 전기 HIL은
  비차단 제외

### AC-02 — 주변장치와 timing output Compatibility

- 상태: **완료** — exact commit `0b7f89283cd82a68a7f3f0910f4fc59b8dd01bfb` 두 보드 HIL PASS
- 난이도: **상** — 추가 instance와 PWM/timer/console 자원 소유권을 profile·DTS와 함께 조정해야 함
- 핵심 구현 범위: `Serial1`, Wire/SPI runtime lifecycle과 제한된 `setPins()`, ADC 8-channel
  metadata·resolution, PWM resolution·frequency, `tone()`/`noTone()`와 Servo
- 명시적 미지원: Wire target/slave와 callback, `requestFrom(..., false)`, `Wire1`, `SPI1`, SPI
  peripheral `attachInterrupt()`/`detachInterrupt()`와 자동 chip-select
- 미지원 항목은 capability와 진단에서 fail-closed하며 가짜 객체나 no-op 성공으로 제공하지 않음
- 핵심 gate: instance conflict·unsupported pin negative, bus·ADC·PWM·tone·Servo build/semantic 및
  필요한 NU54DK HIL

AC-02A는 heap 없는 고정 슬롯 manager와 owner/resource key, 원자적 reserve/commit/rollback/release,
generation/epoch 기반 stale lease 방지, ISR 변경 거부를 구현했다. AC-02B는 이 기반 위에 실제
pinctrl·PM lifecycle 전환과 GPIO handover, Serial1/Wire/SPI/ADC/PWM/tone/Servo 공개 API를 연결했다.
미지원 instance와 mode는 fail-closed로 유지한다. Cross-board Wire 진단에서 확인한 P1.2/P1.3
continuity 불연속은 controller 실패와 분리했다. 최종 3-wire fixture는 공통 GND, peer
P2.5↔DUT P1.12 공유선과 DUT P2.2↔P2.4 local SPI loopback이다. Wire는 DUT 온보드 BQ25186
`0x6A` register `0x0C == 0x41`의 read-only 100/400 kHz repeated-start와 end/rebegin으로 검증했다.
공유선은 peer가 ADC LOW/HIGH를 구동한 뒤 DUT가 같은 A0/P1.12를 PWM20으로 넘기고 peer가 bounded
polling으로 25%·75% duty를 포착했다. Serial1, Wire, SPI, ADC raw 0/3757과 PWM 25%/75%가 모두 PASS했다.
상세 증거는 [AC-02A 소유권 기준선](<../04_검증 기록/26_AC-02A_핀과_주변장치_소유권_기준선.md>)과
[AC-02B runtime 기준선](<../04_검증 기록/27_AC-02B_Peripheral_Analog_runtime_기준선.md>)을 따른다.

### AC-03 — Storage facade와 library 생태계 Compatibility

- 상태: **완료** — exact commit `0b7f89283cd82a68a7f3f0910f4fc59b8dd01bfb` 두 보드 HIL PASS
- 난이도: **상** — persistence·migration·용량 경계와 제3자 library의 compile/runtime 차이를 관리해야 함
- 핵심 범위: Settings/ZMS 위 1024-byte Arduino EEPROM mirror, 전용 32 KiB LittleFS,
  8개 library·29개 예제의 compile/runtime 호환성과 문서
- 핵심 gate: reset persistence·용량·손상·migration negative, 대표 library Arduino CLI compile과
  hardware 의존 항목의 선택 HIL

RC3는 AC-03의 LittleFS `0x16c000..0x174000`과 Settings/ZMS
`0x174000..0x17d000` 주소를 유지하면서, 사용하지 않던 boot reservation과 두 번째 image slot을
기본값에서 제거한다. Loaderless application은 `0x000000..0x16c000`의 1,490,944 byte
(1,456 KiB)를 사용하고 linker와 Arduino maximum size가 같은 경계를 적용한다. 공개 RC1/RC2의
696 KiB dual-slot 표기는 당시 artifact의 역사 기록으로 보존한다.

각 보드는 reset 3회 동안 EEPROM/LittleFS 영속성, 손상 자동 수용 거부, 명시적 복구와 최종
시험 데이터 정리를 통과했다. AC-03의 facade는 `v0.3.0` 내부 storage 호환 계층에 한정한다. 외부 flash, secure storage,
PSA Crypto/Cracen과 update/recovery용 partition은 M23~M26의 `v0.4.0` 범위를 유지한다. Native USB,
DAC, Wi-Fi와 AVR Harvard memory처럼 NU54DK hardware에서 같은 의미를 제공할 수 없는 기능은 AC
완료 조건에 포함하지 않고 명시적 하드웨어 비지원으로 유지한다.

### M22 — `v0.3.0` 릴리스

- 상태: **RC3 memory-contract 교정·공개 재검증 준비**
- 범위: 29개 예제, BLE·Arduino Compatibility 회귀, 동일 PC 격리 clean-room package lifecycle와
  별도 RC index의 `v0.3.0-rc.3` 공개, DTS·linker·Arduino maximum size의 1,456 KiB 경계 일치
- 핵심 gate: AC API matrix·대표 library 결과, exact artifact, 공개 설치 경로 검증, known issues와
  프로젝트 소유자 승인
- 검증 결과: archive 설치본 29/29 compile, 실제 pyOCD Upload, public clean-room 설치·downgrade·
  upgrade·uninstall·reinstall과 exact run leaf cleanup PASS

GATT schema는 Bluetooth 시작 전에 등록하는 수명주기를 기본으로 한다. Bluetooth stack callback은
bounded event를 보존하고 사용자 callback은 Arduino main-thread에서 실행한다.

BLE Mesh, Channel Sounding, ISO와 BLE/802.15.4 multiprotocol은 `v0.3.0` 범위가 아니다.

---

## 8. `v0.4.0` 제품선 — M23~M26

이 제품선은 storage, security, firmware update와 recovery를 다룬다. 상세 API와 fixture는 착수
전에 별도 milestone 문서로 승인한다.

| 단계 | 계획 범위 | 완료 전 필수 경계 |
| --- | --- | --- |
| M23 | Storage 범위, PSA Crypto/Cracen API와 direct 경로 | Key·entropy·lifetime·보안 오용 경계 |
| M24 | Memory layout preset, MCUboot/DFU profile, sysbuild와 partition | Loaderless 기본값 불변, code/linker/size 일치, signed update, rollback과 실패 중단 정책 |
| M25 | TF-M `/ns`, secure boundary와 recovery HIL | Secure/non-secure ownership과 복구 가능성 |
| M26 | `v0.4.0` package와 공개 | Update 실패 복구·migration·security 문서 |

Runtime wrapper가 적절하지 않은 기능은 validated profile 또는 application template로 제공한다.
Update 성공 경로만 확인하고 recovery를 검증하지 않은 상태에서는 제품 지원을 선언하지 않는다.
M24의 Arduino Tools에는 임의 byte 입력 대신 검증한 layout preset을 제공한다. Boot reservation과
dual slot은 실제 MCUboot/DFU profile에서만 선택할 수 있으며, 전문가 overlay도 같은 partition
겹침·linker 경계·migration gate를 우회하지 못한다.

---

## 9. `v0.5.0` 제품선 — M27~M30

이 제품선은 저전력 radio와 network 계층을 다룬다.

| 단계 | 계획 범위 | 완료 전 필수 경계 |
| --- | --- | --- |
| M27 | Radio profile 기반과 NU54DK 802.15.4 적용 build 교정 | Radio resource·clock·NVMC와 profile ownership |
| M28 | IEEE 802.15.4와 ESB direct/profile 예제 | Physical TX/RX HIL과 오류·channel 경계 |
| M29 | OpenThread facade·raw handle와 network 계층 | Join, radio, UDP/client runtime과 reset recovery |
| M30 | `v0.5.0` package와 공개 | Profile migration, coexistence 제약과 network HIL |

M17의 OpenThread NU54DK Build PASS는 join이나 network runtime 지원이 아니다. IEEE 802.15.4와
ESB, BLE를 함께 쓰는 구성은 MPSL resource·coexistence gate 전까지 상호 배타 profile이다.

---

## 10. `v0.6.0` 제품선 — M31~M34

이 제품선은 Matter product foundation과 application 경계를 다룬다.

| 단계 | 계획 범위 | 완료 전 필수 경계 |
| --- | --- | --- |
| M31 | Partition, factory data와 provisioning | NU54DK storage layout과 production data 정책 |
| M32 | Matter application template와 Arduino UX | Core wrapper와 독립 application의 책임 분리 |
| M33 | Commissioning, fabric와 runtime HIL | 실제 controller·network·reset persistence |
| M34 | `v0.6.0` package와 공개 | Provisioning, migration, security 운영과 공개 gate |

Matter application build와 commissioning PASS는 CSA 인증 획득을 의미하지 않는다. 인증, 제품
보안 운영과 credential 관리 책임은 Arduino API 지원과 별도로 결정한다.

---

## 11. 공통 완료 규칙

마일스톤을 `완료`로 바꾸려면 해당 범위에 필요한 다음 증거를 연결한다.

1. 고정 source, board, NCS, Zephyr와 Toolchain identity
2. Public API signature와 compile/link contract
3. 정상·경계·오류·negative semantic test
4. Profile, Kconfig, DTS와 resource conflict 검증
5. 필요한 NU54DK HIL과 fixture 경계
6. Arduino CLI/IDE example과 package 사용자 경로
7. 알려진 제약, 범위 제외와 복구 절차
8. Version별 release artifact와 공개 승인

Hardware 의존 기능은 build 성공만으로 완료하지 않는다. 프로젝트 소유자가 명시적으로 승인한
비지원·NOT RUN 범위는 지원표에 정확히 표시하고 해당 milestone의 비차단 제외가 될 수 있다.
다만 AC-01~AC-03에서 구현 목표로 확정한 항목은 자동으로 비차단 제외하지 않는다. 안전한 구현,
하드웨어 비적용 판정 또는 별도 scope 개정 승인 전까지 해당 AC package와 M22를 완료하지 않는다.

---

## 12. 변경 관리

- 단계 상태를 바꿀 때 완료 기준과 증거 링크를 함께 갱신한다.
- 상세 수치와 실행 log는 검증 기록에만 추가하고 master roadmap에 복제하지 않는다.
- API 지원 범위가 바뀌면 API matrix와 필요한 예제·시험을 같은 변경에서 갱신한다.
- AC package 상태를 바꿀 때 해당 API별 난이도·의미 차이·HIL 범위와 M22 선행조건을 함께 갱신한다.
- Board 변경은 별도 board 저장소에서 검증한 뒤 Core의 gitlink만 의도적으로 갱신한다.
- NCS upgrade는 기존 v3.4.0 기준선을 덮어쓰지 않고 별도 호환성 평가와 migration으로 결정한다.
- RC나 stable version 이름으로 다른 byte의 artifact를 다시 만들지 않는다.
- M23~M34 상세 범위는 착수 시 별도 milestone 문서와 machine-readable coverage record로 고정한다.
- AC-01~AC-03은 `v0.3.0` 전용 하위 작업 ID이며 이후 전역 M 번호를 대체하거나 재번호화하지 않는다.
