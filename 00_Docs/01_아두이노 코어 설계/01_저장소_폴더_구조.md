# NU54DK Arduino Core — 저장소 구조와 소유권

| 항목 | 내용 |
| --- | --- |
| 문서 ID | ARCH-REPOSITORY-001 |
| 문서 개정 | 4.8 |
| 문서 상태 | v0.4.1 stable 구조와 후속 개발 소유권 안내 |
| 적용 제품 버전 | `v0.4.1` stable + `main` 후속 개발 |
| 최종 갱신일 | 2026-09-12 |
| 작성자 | Quantum / NUCODE |
| 기준 보드 정의 | [Nucode01/NU54DK_Zephyr_DTS](https://github.com/Nucode01/NU54DK_Zephyr_DTS) |

---

## 1. 설계 원칙

1. NU54DK의 물리 GPIO, pinctrl, 주변장치와 runner는 보드 저장소가 단독으로 소유한다.
2. Core는 `board_package/NU54DK_Zephyr_DTS` 서브모듈을 읽기 전용 build 입력으로 사용한다.
3. Arduino Variant는 논리 pin과 capability를 제공하고 물리 DTS의 두 번째 원본이 되지 않는다.
4. Arduino Core, 기능 library, build adapter와 package tool의 책임을 분리한다.
5. Native Zephyr reference는 `samples/zephyr`, Arduino 사용자 예제는 `libraries/*/examples`에 둔다.
6. 한 예제의 복사본을 여러 경로에 두지 않는다.
7. 보드별 HIL은 `tests/hil/<board>` 아래에 둔다.
8. 임시 빌드 산출물과 cache는 Git에 포함하지 않는다. 계약으로 추적하는 정적 생성 table·문서는 생성기와 함께 유지한다.
9. 공개 stable archive는 exact source/tooling commit에 고정한다.
10. 구현되지 않은 미래 디렉터리를 빈 placeholder로 미리 만들지 않는다.

---

## 2. 핵심 구조와 파일 찾기

지원 stable은 `v0.4.1` 하나이며 main은 v0.4.0 기능 기준선의 완료 기록과 후속 개발 기준을 유지한다. 아래는
책임 경계를 찾기 위한 요약이다. 개별 source·예제·검증 기록의 전체 목록을 복제하지 않으며,
실제 파일은 아래 읽기 전용 검색 명령으로 확인한다.

```text
NU54DK_Arduino_Core/
├─ .github/workflows/              # software CI와 release orchestration
├─ 00_Docs/
│  ├─ README.md                    # 문서 색인
│  ├─ TODO_v0.4.0.md               # v0.4.0 완료 상태와 실행 기록
│  ├─ 00_사전 리서치/              # 선택 근거
│  ├─ 01_아두이노 코어 설계/       # 구조·API 범위·마일스톤·리팩토링
│  ├─ 02_빌드 설계/                # build·설치·package·CI
│  ├─ 03_펌웨어 설계/              # runtime·peripheral·BLE·Storage 계약
│  ├─ 04_검증 기록/                # 실행 판정과 evidence 색인
│  └─ 05_릴리스/<version>/         # 버전별 사용자 문서·불변 공개 기록
├─ board_package/NU54DK_Zephyr_DTS/ # 읽기 전용 board submodule
├─ coverage/ncs-v3.4.0/            # manifest·기능 record·생성 요약
├─ cores/arduino/
│  ├─ Arduino.h                    # Arduino 공개 진입점
│  ├─ NUCODEPeripheral.h           # concrete peripheral 확장
│  ├─ nucode/                      # 공개 fabric·inventory 계약
│  ├─ internal/                    # 비공개 runtime·자원 수명주기
│  └─ generated/                   # manifest로 생성한 table
├─ variants/nu54dk/                # 논리 pin·capability·route와 contract
│  └─ profiles/                    # standard/ble과 v0.4 fabric 구성
├─ libraries/<library>/           # v0.4.0 stable library 9개
│  ├─ src/                         # 해당 공개 API와 구현
│  ├─ zephyr/                      # feature가 있는 library의 fragment
│  └─ examples/<Sketch>/           # 사용자 Arduino 예제의 단일 원본
├─ dts/                           # Core binding·Arduino metadata include
├─ zephyr/                        # module.yml·Kconfig·CMake와 내부 include
├─ samples/zephyr/                # west-native reference·PoC
├─ tests/
│  ├─ host/                       # host 계약·도구 회귀
│  ├─ zephyr/                     # target build·semantic contract
│  ├─ arduino-cli/                # 실제 CLI source discovery·compile
│  └─ hil/nu54dk/                 # 보드 HIL runner·fixture·시험 계획
├─ tools/                         # ci·coverage·peripheral·builder·release 도구
├─ packaging/boards-manager/      # archive·index·checksum·SBOM
├─ third_party/ArduinoCore-API/   # 고정 upstream snapshot
├─ boards.txt / platform.txt      # Arduino platform recipe·metadata
├─ post_install.bat              # 설치 진입점
├─ package_nucode_nu54dk_index.json
├─ package_nucode_nu54dk_preview_index.json
└─ README.md / LICENSE
```

| 찾을 작업 | 진입점 |
| --- | --- |
| 현재 문서·완료 상태 | [문서 색인](../README.md), [v0.4.0 완료 기록](../TODO_v0.4.0.md) |
| Core build adapter | `tools/nu54-builder/src/nu54_builder.py`; 구현은 같은 경로의 `nu54_builder_impl/` |
| Boards Manager package | `packaging/boards-manager/nu54_package.py`; 구현은 같은 경로의 `nu54_package_impl/` |
| Inventory·Serial·System·시험 계획 생성 | [생성기 안내](../../tools/peripheral/README.md) |
| v0.4 HIL 계획과 실행 경계 | `tests/hil/nu54dk/v04_test_plan.json`, `v04_t13_plan.py`, `v04_pair.py` |
| 버전별 설치·지원 범위 | [릴리스 색인](../05_릴리스/README.md) |

저장소 루트에서 실행하는 파일 검색 예시다. 빌드·flash·HIL을 실행하지 않는다.

```powershell
rg --files cores/arduino variants/nu54dk
rg --files libraries -g "*.ino"
rg --files tests/hil/nu54dk -g "v04*"
rg --files 00_Docs -g "*.md"
```

---

## 3. 경로별 소유권

| 경로 | 책임 | 포함하지 않는 것 |
| --- | --- | --- |
| `00_Docs/` | 결정, 설계, 검증과 릴리스 기록 | 생성 build artifact |
| `00_Docs/01_아두이노 코어 설계/14_리팩토링/` | 장기 진단, R00~R14 실행 순서·작업 양식과 활성 체크리스트 | 제품 T 상태·실기 PASS의 복제 또는 임시 다운로드 묶음 |
| `board_package/NU54DK_Zephyr_DTS/` | 보드 DTS, pinctrl, runner, 회로 문서 | Core 전용 local patch |
| `coverage/ncs-v3.4.0/` | exact upstream pin, 기능별 제공 경로·상태와 검증 결과 | NCS 전체 지원이라는 포괄 선언 |
| `cores/arduino/` | Arduino runtime과 공통 API backend | 기능별 대형 subsystem wrapper |
| `cores/arduino/generated/` | manifest에서 생성한 compile-time table | 사람이 직접 수정한 상태 선언 |
| `cores/arduino/internal/` | 고정 슬롯 소유권, GPIO handover, runtime pinctrl/PM과 PWM allocator | Sketch가 직접 포함하는 공개 API |
| `variants/nu54dk/` | Arduino 논리 pin·capability, UART20 고정 registry, runtime route와 M23 peripheral manifest | 보드 물리 정의 복사본 또는 SoC가 허용하지 않는 임의 pin remap |
| `libraries/NUCODE_NU54DK/` | 보드 진입점, board/system API와 공통 예제 | Zephyr 보드 DTS |
| `libraries/NUCODE_BLE/` | v0.2 NUS Stream과 v0.3 BLE Core/GAP·범용 GATT 공개 library·예제 | BLE 보안·HID의 독립 feature 소유권, Mesh·Channel Sounding·ISO·검증되지 않은 multiprotocol |
| `libraries/NUCODE_BLE_Security/` | v0.3 pairing·bonding·BAS/DIS·HID 공개 API, feature와 예제 | 공통 stack lifecycle, GAP/GATT 구현의 복제 |
| `libraries/SPI`, `libraries/Wire` | 해당 Arduino API 진입점과 기능 예제 | 자동으로 추정한 CS·두 번째 bus 객체 |
| `libraries/Servo` | PWM22 기반 최대 4-channel Servo API와 Sweep 예제 | motor 전원 공급 또는 임의 PWM block 공유 |
| `libraries/EEPROM` | 1024-byte Settings/ZMS mirror, CRC와 명시적 commit 예제 | 물리 EEPROM 또는 ISR-safe storage |
| `libraries/LittleFS` | 전용 32 KiB partition의 비파괴 filesystem facade와 예제 | 외부 flash, SD 또는 임의 partition 교체 |
| `libraries/NUCODE_Peripheral_Fabric/` | v0.4 `fabric` profile의 직접 Peripheral API 진입점·capability·예제 | `standard`/`ble` singleton과의 혼합, QDEC 반복 manual read/clear의 무손실 보증 |
| `samples/zephyr/` | Arduino CLI 없이 빌드하는 Native Zephyr reference | 사용자 Arduino 예제 |
| `tests/host/` | filesystem, parser, package와 build logic unit test | 물리 hardware PASS |
| `tests/zephyr/` | ztest/Twister와 target build contract | 외부 fixture 추정 |
| `tests/arduino-cli/` | IDE/CLI source discovery, compile와 upload contract | Core 내부 unit test |
| `tests/hil/nu54dk/` | NU54DK 실기 flash/UART/peripheral test | 다른 보드 공통 test |
| `tools/coverage/` | coverage schema·hash·경로 검증과 deterministic 요약 | 사람이 수동으로 고친 generated 통계 |
| `tools/peripheral/` | M23 inventory와 M24 serial-fabric schema, exact DTS/source 검증·문서 생성 | 지원 상태의 수동 복제 |
| `tools/ci/` | 고정 입력 기반 host/target build와 evidence 생성 | 외부 library vendoring |
| `tools/nu54-builder/` | Arduino build graph를 Full Zephyr build로 변환 | NCS/Zephyr 자체 재배포 |
| `tools/nu54-prerequisites/` | Nordic 공식 prerequisite의 exact-pin 설치·검증 | 임의 third-party binary bundle |
| `tools/release/` | 동결된 릴리스 gate와 evidence | 다음 버전의 자동 호환 가정 |
| `packaging/boards-manager/` | archive, index, checksum, SBOM와 package 검증 | source tree build 출력 |
| `third_party/ArduinoCore-API/` | 고정 upstream snapshot | NUCODE local patch |

---

## 4. Arduino 예제 단일 원본

Arduino IDE는 platform bundled library의 `examples`를 열거한다. 따라서 사용자 예제는 다음
경로만 사용한다.

```text
libraries/<소유-library>/examples/<Sketch>/<Sketch>.ino
```

루트 `examples/`는 사용하지 않는다. package allowlist와 host/CLI test도 이 규칙을 강제한다.

정식 `v0.4.0`은 library 9개와 예제 30개를 제공한다. `Standard peripherals` 22개, BLE 7개,
`fabric` 전용 `FabricCapabilities` 1개이며 공개 설치본 전체 빌드를 T24에서 확인했다.
이전 `v0.3.0`의 library 8개·예제 29개와 `v0.2.0`의 library 4개·예제 14개는 각 공개 자산의
역사적 기준으로 보존한다. 전체 예제 목록과 profile 선택은
[구성 프로필과 예제 배포](<../02_빌드 설계/07_구성_프로필과_Arduino_예제_배포.md>)를 따른다.

기능별 예제 소유 원칙은 다음과 같다.

- 보드 GPIO, pin 역할, console과 M15 board/system API: `NUCODE_NU54DK`
- 버스 API: `Wire`, `SPI`
- BLE Core/GAP·GATT·NUS: `NUCODE_BLE`
- BLE security·표준 profile·HID: `NUCODE_BLE_Security`
- Servo pulse: `Servo`
- byte-addressed persistent mirror: `EEPROM`
- internal filesystem facade: `LittleFS`
- 검증된 직접 Peripheral Fabric과 capability 확인: `NUCODE_Peripheral_Fabric`
- Zephyr driver 자체 검증: `samples/zephyr` 또는 `tests/zephyr`

---

## 5. 보드 정의와 회로 문서

Core가 사용하는 보드 루트는 다음 하나뿐이다.

```text
-DBOARD_ROOT=<Core>/board_package/NU54DK_Zephyr_DTS
```

보드 변경은 다음 순서를 따른다.

1. `NU54DK_Zephyr_DTS` 저장소에서 수정·검증·commit한다.
2. Core에서는 새 보드 commit을 검토한다.
3. 상위 gitlink만 의도적으로 갱신한다.
4. Core/Variant/API 회귀를 실행한다.

회로도 PDF도 보드 저장소를 단일 원본으로 사용한다. Core `00_Docs`에 동일 PDF 복사본을 두지
않는다.

---

## 6. Source, generated와 release artifact

| 종류 | 위치 | Git |
| --- | --- | --- |
| 직접 관리 source | 위 트리의 코드·문서 | 포함 |
| 보드 source | submodule gitlink와 별도 저장소 | gitlink만 포함 |
| build tree/cache | `build/`, 외부 cache root | 제외 |
| Python cache | `__pycache__`, `*.pyc` | 제외 |
| Boards Manager archive | release asset/검증 임시 출력 | source Git에서 제외 |
| package index | 저장소 루트 stable/preview JSON | 의도한 채널만 포함 |
| 대용량 HIL evidence | CI/release artifact | Git에는 요약·hash만 포함 |

과거 공개 `v0.1.0`은 commit `5dbc5e37270e477d21f578dd877f4b5226b44a0d`와 그 시점의
패키징 도구에 고정한다. 후속 main에서 같은 버전 archive를 다시 만들지 않는다.

---

## 7. 버전별 구조의 도입 이력

다음 경로는 M12~M18에서 실제 파일이 생긴 `v0.2.0` 정식 기준선이다.

```text
.github/workflows/                  # software CI와 release orchestration
tools/ci/                           # CI가 호출하는 재현 가능한 로컬 진입점
variants/nu54dk/profiles/<id>/      # 검증된 curated profile
libraries/<feature>/zephyr/         # 허용된 feature manifest와 fragment
tests/host/test_m13_profiles.py      # profile schema, 병합과 conflict 회귀
coverage/ncs-v3.4.0/                # M17 machine-readable coverage ledger
tools/coverage/                     # ledger 검증·요약 생성
tests/zephyr/m17_sensor_direct/      # sensor direct build-only contract
tests/arduino-cli/m17_adafruit_lsm6ds_compile/ # 외부 LSM6DS3TR-C compile fixture
```

profile과 feature library 구조는
[구성 프로필과 Arduino 예제 배포](../02_빌드%20설계/07_구성_프로필과_Arduino_예제_배포.md)를
따른다.

`v0.3.0`은 AC-01~AC-03 Arduino Compatibility와 M19~M21 BLE 작업을 M22에서 통합했다.
API별 실기 수치와 exact source는 [v0.3.0 마일스톤](07_v0.3.0_구현_마일스톤.md)에서 확인한다.
저장소 구조 문서는 실행 로그를 복제하지 않고 파일 소유권만 설명한다.
`NUCODE_BLE`는 NUS/Core/GAP/GATT, `NUCODE_BLE_Security`는 보안·표준 profile·HID를 소유한다.

`v0.4.0`의 M23은 `variants/nu54dk/peripheral-manifest.json`을 75개 hardware identity의
machine-readable 단일 원본으로 추가했다. `tools/peripheral/verify_m23_inventory.py`가 exact NCS
DTS hash·node와 Core source를 검사하고 public runtime table과 문서 matrix를 결정적으로 생성한다.
M24 작업 1은 `variants/nu54dk/serial-fabric-contract.json`과
`tools/peripheral/verify_m24_serial_contract.py`로 5개 serial block, 23개 personality, 핀 bank,
고급 API와 DMA lifecycle을 고정한다. M24~M26은 이 상태 축을 실제 구현·semantic·HIL 결과로 갱신한다.
리팩토링 진단과 실행계획은 `00_Docs/01_아두이노 코어 설계/14_리팩토링/`에 둔다. 최상위
`00_Docs/`에 버전이 붙은 임시 문서 묶음을 별도로 유지하지 않으며, 제품 진행 상태는 기존
`TODO_v0.4.0.md`에서 완료 결과를 확인한다. Master roadmap은 제품 순서와 후속 완료 기준을 소유한다.

```text
cores/arduino/, variants/nu54dk/     # AC-01 Core·GPIO·시간과 AC-02 dynamic peripheral/analog backend
dts/nucode/                          # Arduino pin metadata와 runtime device profile include
libraries/                           # AC-02 peripheral/Servo, AC-03 Storage/library와 BLE 공개 진입점
libraries/NUCODE_BLE/examples/       # NUS, GAP와 custom GATT 예제
libraries/NUCODE_BLE_Security/       # M21 보안·BAS/DIS·HID API와 SecureKeyboard 예제
tests/host/                          # Compatibility·BLE API, 수명주기와 negative 계약
tests/zephyr/                        # profile, target build·semantic test
tests/hil/nu54dk/ac02b_peripheral.py # nonce/exact image/UID/COM fail-closed AC-02B HIL
tests/hil/nu54dk/                    # 핀·주변장치·storage와 두 보드 BLE 회귀
00_Docs/01_아두이노 코어 설계/
└─ 07_v0.3.0_구현_마일스톤.md       # AC-01~AC-03, M19~M22 단일 원본
```

GATT service와 characteristic은 Bluetooth stack 시작 전에 선언·등록하는 수명주기를 기본으로
한다. 실행 중 임의 schema 변경, Bluetooth callback 문맥에서의 사용자 code 직접 실행과
무제한 동적 할당은 계획 범위에 포함하지 않는다.

Compatibility 작업선은 보드 DTS가 소유한 물리 pin·peripheral을 Core에 다시
하드코딩하지 않는다. `Wire1`·`SPI1`은 승인된 board route가 없어 가짜 객체로 만들지 않는다.
AIN0~7 이름은 모두 제공하되 UART20/PMIC system owner와 SW0/LED 부하를 우회하지 않는다.

---

## 8. 릴리스 문서 구조

모든 릴리스 문서는 버전 디렉터리를 사용한다. 과거 평면 번호 파일도 내용과 Git 이력은 보존한
채 `v0.1.0-rc.1`, `v0.1.0-rc.2`, `v0.1.0` 디렉터리로 정리했다. 파일명은 역할이 같은 문서를
찾기 쉽도록 `README.md`, `RELEASE_NOTES.md`, `KNOWN_ISSUES.md`, `MIGRATION.md`,
`TROUBLESHOOTING.md`로 통일한다. 철회·중단처럼 해당 버전에만 있는 증거는 별도 역할 이름을 쓴다.

```text
00_Docs/05_릴리스/<version>/
├─ README.md
├─ RELEASE_NOTES.md
├─ KNOWN_ISSUES.md
├─ MIGRATION.md
└─ TROUBLESHOOTING.md
```

필요한 버전은 위 다섯 파일에 `TESTING.md`, `WITHDRAWAL.md` 또는 `CLEANROOM_ABORT.md`처럼
그 버전에만 해당하는 증거를 더한다. RC와 이전 stable은 공개된 역사 기록으로 동결하며 현재
stable 문서가 설치·지원 경계를 소유한다. Stable 승격 뒤에도 이전 버전 문서를 덮어쓰지 않는다.

---

## 9. 이름과 버전 규칙

- 제품 릴리스: 문장과 Git tag에서 `v0.4.0`; package metadata에서 앞의 `v`를 생략
- 마일스톤: `M12`처럼 제품 버전과 독립된 연속 번호
- Compatibility 작업선: `AC-01`처럼 `v0.3.0` 내 Arduino 호환성 단계를 식별
- 문서 개정: `1.0`, `2.0`처럼 제품 SemVer와 분리
- 내부 schema/cache 경로: `schema v1`, `cache schema v1`처럼 대상을 함께 표기
- bare `v1`을 제품 버전 의미로 사용하지 않음
- 검증 기록 파일은 완료 뒤 역사적 이름과 결과를 유지

---

## 10. 구조 변경 체크리스트

- [ ] 보드 서브모듈 내부를 수정하지 않았는가
- [ ] 같은 example 또는 회로 문서를 복제하지 않았는가
- [ ] 새 public library에 `library.properties`, license와 예제가 있는가
- [ ] 예제 폴더명과 주 `.ino` 이름이 같은가
- [ ] package allowlist와 SBOM에 새 runtime 파일이 포함되는가
- [ ] host, Arduino CLI, Zephyr와 필요한 HIL test 경로를 갱신했는가
- [ ] 문서 index와 상대 link 검사가 통과하는가
- [ ] 공개 stable artifact identity를 변경하지 않았는가
